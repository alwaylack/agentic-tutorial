import { useSSRContext, resolveComponent } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
const __pageData = JSON.parse('{"title":"第 15 章 · 远程舰队、Relay 与运维工具箱","description":"","frontmatter":{},"headers":[],"relativePath":"firstmate/ch15.md","filePath":"firstmate/ch15.md","lastUpdated":1787834555000}');
const __default__ = { name: "firstmate/ch15.md" };
const _sfc_main = /* @__PURE__ */ Object.assign(__default__, {
  __ssrInlineRender: true,
  setup(__props) {
    const quiz = [
      {
        question: "关于远程主机的 SSH 通道，下列哪项是 FirstMate 明确禁止的？",
        options: [
          "使用公钥认证",
          "开启 SSH agent forwarding",
          "配置 SSH alias",
          "严格主机密钥校验"
        ],
        answer: 1,
        explain: "官方要求普通公钥认证 + 严格主机密钥校验，且绝不开启 agent forwarding；fm-on.sh 每次调用都会主动禁用它。"
      },
      {
        question: "fm-remote-entrypoint.sh 接受什么样的输入？",
        options: [
          "任意 shell 命令字符串",
          "仅编码后的 argv，且仅限真正可执行的 bin/fm-*.sh 文件",
          "任意 bin/ 下的脚本路径加自由参数",
          "JSON 配置文件"
        ],
        answer: 1,
        explain: "入口点只接受编码 argv 且仅限真正的 fm-*.sh 可执行文件，从不接受 shell 命令字符串——这是远程攻击面的关键收窄。"
      },
      {
        question: "远程路由供给过程中 SSH 返回退出码 255 时，系统会怎么做？",
        options: [
          "立即回滚新路由",
          "保留路由，因为远程完成与否未知，必须稍后在同一主机上 reconcile",
          "自动在本地重建一份 secondmate",
          "删除 data/secondmates.md 里的所有记录"
        ],
        answer: 1,
        explain: '已知失败会回滚；唯独 SSH 255 保留路由——远程状态未知时不能贸然回滚也不能本地顶替，这正是"永不降级为本地替换"原则的体现。'
      },
      {
        question: "关于 Relay 的 promised final public reply，正确的理解是？",
        options: [
          "存在对话记忆里，会话压缩后自然消失",
          "是持久状态，由磁盘记录 reconcile，重启或压缩会话都不会丢",
          "由任意 secondmate 代发即可",
          "每次会话开始时重新生成"
        ],
        answer: 1,
        explain: "承诺的最终公开回复是 durable state 而非 conversation memory；且只有持有 Relay 同意与线程绑定的 home 才能发帖，绝不委托船员或 secondmate。"
      }
    ];
    return (_ctx, _push, _parent, _attrs) => {
      const _component_Quiz = resolveComponent("Quiz");
      _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="第-15-章-·-远程舰队、relay-与运维工具箱" tabindex="-1">第 15 章 · 远程舰队、Relay 与运维工具箱 <a class="header-anchor" href="#第-15-章-·-远程舰队、relay-与运维工具箱" aria-label="Permalink to &quot;第 15 章 · 远程舰队、Relay 与运维工具箱&quot;">​</a></h1><blockquote><p>本章目标：掌握把 secondmate home 整体放到 SSH 可达远程主机的方法，理解 Relay 公开提及应答的安全模型，并学会用 backlog 契约与工具带脚本维持舰队日常运转。</p></blockquote><h2 id="_15-1-远程-secondmate-整个-home-搬上另一台机器" tabindex="-1">15.1 远程 secondmate：整个 home 搬上另一台机器 <a class="header-anchor" href="#_15-1-远程-secondmate-整个-home-搬上另一台机器" aria-label="Permalink to &quot;15.1 远程 secondmate：整个 home 搬上另一台机器&quot;">​</a></h2><p>第 14 章的 secondmate 与主 home 同机。当船队规模扩大、或需要隔离算力时，可以把<strong>整个 secondmate home</strong> 放到一台 SSH 可达的远程主机上。官方文档（<code>docs/remote-secondmates.md</code>）定义了完整的 setup、路由、迁移、恢复与安全行为。</p><h3 id="前置-ssh-通道的硬性要求" tabindex="-1">前置：SSH 通道的硬性要求 <a class="header-anchor" href="#前置-ssh-通道的硬性要求" aria-label="Permalink to &quot;前置：SSH 通道的硬性要求&quot;">​</a></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1. 在主账号的 ~/.ssh/config 中配置普通 SSH alias</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Host</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fleet-box</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  HostName</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 203.0.113.10</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  User</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fleet</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  IdentityFile</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ~/.ssh/id_ed25519</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2. 使用普通公钥认证 + 严格主机密钥校验；尽量用专用远程账号</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ssh-keygen</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -t</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ed25519</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ssh-copy-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fleet-box</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>三条安全红线：</p><ul><li><strong>绝不开启 agent forwarding</strong>——<code>fm-on.sh</code> 每次调用都会主动禁用它、转发 setup 与配置的 <code>SendEnv</code> 模式；</li><li><strong>有界死链检测</strong>——<code>fm-on.sh</code> 武装 SSH dead-peer detection，主机重启或断链会在有界窗口内失败，而不是无限挂起；</li><li>远程主机上要自备工具链、所选 worker runtime、所选会话后端与可用凭据。</li></ul><h3 id="远程入口与作业-worker" tabindex="-1">远程入口与作业 worker <a class="header-anchor" href="#远程入口与作业-worker" aria-label="Permalink to &quot;远程入口与作业 worker&quot;">​</a></h3><p>在远程主机上以绝对路径克隆 firstmate，并把固定入口暴露到该账号的非交互 SSH <code>PATH</code>：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 在远程主机上执行</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ~/.local/bin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ln</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -s</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /absolute/path/to/firstmate/bin/fm-remote-entrypoint.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">      ~/.local/bin/fm-remote-entrypoint.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>入口点只接受<strong>编码后的 argv</strong>，且仅限真正可执行的 <code>bin/fm-*.sh</code> 文件——它从不接受 shell 命令字符串。此后每条命令都会校验远程作业 worker、暂存 argv 与 stdin、等待结果、分别回传 stdout/stderr/退出码。</p><p>在 macOS 上，这个 worker 是 Aqua 会话里的 LaunchAgent：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/Library/LaunchAgents/dev.firstmate.remote-job.plist</span></span>
<span class="line"><span>日志目录: ~/Library/Logs/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>worker 的行为契约值得记住：<strong>一次只跑一个暂存作业</strong>；只要队列里来了非长轮询命令，就立刻抢占正在运行的 reply long-poll——交互命令永远不会排在轮询窗口后面。worker 还会自我了断：一旦它的代码根不再是 firstmate 检出（比如是从 worktree 启动的），就自动停止，<code>bin/fm-remote-job-reap-orphans.sh</code> 负责清走这类遗留 worker。</p><h3 id="非交互工具发现-不读-shell-启动文件" tabindex="-1">非交互工具发现：不读 shell 启动文件 <a class="header-anchor" href="#非交互工具发现-不读-shell-启动文件" aria-label="Permalink to &quot;非交互工具发现：不读 shell 启动文件&quot;">​</a></h3><p>远程主机上从不运行登录 shell，所以 <code>~/.profile</code>、<code>~/.bashrc</code>、<code>~/.zshrc</code> 对运行时 <code>PATH</code> <strong>毫无贡献</strong>。worker 的 PATH 由文件系统发现构建：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;remote-root&gt;/bin → ~/.local/bin → nvm default bin → asdf/mise shims</span></span>
<span class="line"><span>→ Nix 目录 → Homebrew 目录 → /usr/bin:/bin:/usr/sbin:/sbin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>若某个工具只能靠版本管理器的 shell hook 找到，<code>fm-remote-doctor.sh --fix</code> 可以在 <code>~/.local/bin</code> 生成 firstmate 自有的 wrapper（绝不覆盖非自有文件、绝不装包）。手动写一个也是同样形状：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cat</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ~/.local/bin/tasks-axi</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;SH&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">#!/usr/bin/env bash</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">tool_bin=&quot;$HOME/.nvm/versions/node/v22.12.0/bin&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">PATH=&quot;$tool_bin:$PATH&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">exec &quot;$tool_bin/tasks-axi&quot; &quot;$@&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">SH</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">chmod</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> +x</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ~/.local/bin/tasks-axi</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h2 id="_15-2-供给远程路由与日常启停" tabindex="-1">15.2 供给远程路由与日常启停 <a class="header-anchor" href="#_15-2-供给远程路由与日常启停" aria-label="Permalink to &quot;15.2 供给远程路由与日常启停&quot;">​</a></h2><p>先在本地填好普通 secondmate 章程（charter），然后一条命令供给远程路由：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">bin/fm-remote-home-seed.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">ssh-alia</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">s</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">remote-roo</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">t</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">remote-hom</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    myapp=https://github.com/you/myapp.git</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 或不带项目：把最后一个参数换成 --no-projects</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>seed 过程做了这些事：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 在 data/secondmates.md 记录 host: root: home: 三元组</span></span>
<span class="line"><span>2. 对主机做只读就绪检查；发现缺口自动 --fix，再跑第二次只读检查定论</span></span>
<span class="line"><span>3. 发送有界 manifest，让远程主机自行克隆它的 firstmate home 与项目 origin</span></span>
<span class="line"><span>4. 写入 .fm-secondmate-parent 持久记录（标明 parent 路由是 local 还是 remote）</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>几个关键安全语义：</p><ul><li><strong>项目在远程主机上自行克隆</strong>，不从主机器拷贝；每个项目的 origin 必须从远程账号可达；</li><li><strong>未注册或 local-only 的项目会被拒绝</strong>供给——交付模式仍以主机器 <code>data/projects.md</code> 为准；</li><li>供给失败会<strong>回滚新路由</strong>；唯独 SSH 退出码 255 时保留路由，因为远程完成与否未知，必须在同一主机上 reconcile；</li><li>主 home 侧的持久登记只含路由与 <code>data/&lt;id&gt;</code> 下的章程简报，launch 记录要等真正启动才创建。</li></ul><p>日常启停与本地 secondmate 用同一条命令：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">bin/fm-spawn.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --secondmate</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>所有远程 secondmate 在同一主机上共享 Herdr 空间 <code>fm-remote</code>，各自保留独立的 <code>2ndmate-&lt;id&gt;</code> workspace。显式要求其他后端会被拒绝——远程主机也会拒绝。主机就绪状态漂移后的启动会直接失败并打印 doctor 的缺口文本，而不是留下半成品端点。</p><p><strong>恢复铁律</strong>：不可用的远程路由<strong>永远不会被降级为本地替换</strong>。重启后的 reconcile 会确认已死亡的 secondmate agent 并恢复它们，但对「读不到的远程」保持原样等待，绝不在本地悄悄重建一份冒名顶替。</p><h2 id="_15-3-relay-让舰队应答公开提及" tabindex="-1">15.3 Relay：让舰队应答公开提及 <a class="header-anchor" href="#_15-3-relay-让舰队应答公开提及" aria-label="Permalink to &quot;15.3 Relay：让舰队应答公开提及&quot;">​</a></h2><p>Relay（旧称 X mode，标识符仍保留 <code>FMX_</code>/<code>x-</code>/<code>fm-x-</code> 拼写）是可选的公开提及应答集成。它的安全模型非常克制：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 默认惰性（ships inert）——不放 token 就没有任何行为变化</span></span>
<span class="line"><span>2. 在 home 私有的 .env 中放入 FMX_PAIRING_TOKEN 即为选择加入</span></span>
<span class="line"><span>3. token 是对「公开回复 + 正常可逆生命周期动作」的同意，</span></span>
<span class="line"><span>   不是对破坏性/不可逆/安全敏感动作的授权——那些仍需可信通道确认</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># .env（gitignored，永不入库）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">FMX_PAIRING_TOKEN</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">your-pairing-token</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>工作方式：<code>x-mention &lt;request_id&gt;</code> 或 <code>x-mode-error</code> 唤醒检查到达时，第一副手加载 <code>fmx-respond</code> 技能，由它负责分类、公共安全策略、回复或忽略、任务关联与后续跟进。即使 home 没有舰队工作，也必须保持活跃监督循环，否则提及无法唤醒它。</p><p>三条必须刻进脑子的规则：</p><ol><li><strong>承诺过的最终公开回复是持久状态，不是对话记忆</strong>——重启或压缩会话都不能弄丢它，磁盘上的记录负责 reconcile；</li><li><strong>只有持有 Relay 同意与线程绑定的那个 home 才能发帖</strong>——绝不指派 secondmate 或船员去找线程、发回复；</li><li>上线前可用 <strong>dry-run preview</strong>：把「将要发出的回复与忽略决定」先记录在本地，确认无误再 go-live。</li></ol><p>对每个 Relay 关联的终态结果：有已输入的公开承诺就走 promised-final reconciliation，否则在 teardown 前发出最终完成跟进——7 天内最多三条 public-safe 跟进，只为真实里程碑与最终结果。</p><h2 id="_15-4-backlog-契约-持久工作队列" tabindex="-1">15.4 Backlog 契约：持久工作队列 <a class="header-anchor" href="#_15-4-backlog-契约-持久工作队列" aria-label="Permalink to &quot;15.4 Backlog 契约：持久工作队列&quot;">​</a></h2><p><code>data/backlog.md</code> 是舰队的持久队列，三条契约要记住：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 只记工作项，不记智能体——secondmate 永远不出现在主 backlog 里</span></span>
<span class="line"><span>2. 派给 secondmate 的工作记录在它自己 home 的 backlog</span></span>
<span class="line"><span>3. 决策就是&quot;留给 captain 的任务&quot;：</span></span>
<span class="line"><span>   tasks-axi hold &lt;id&gt; --reason &quot;&lt;reason&gt;&quot; --kind captain [--until &lt;date&gt;]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>每次派发、完成、决策都要更新 backlog；每次 teardown 与心跳后重新评估排队工作，依赖与时间闸门都清了才派发。后端默认是 tasks-axi（配置在 <code>.tasks.toml</code>）；把 <code>config/backlog-backend</code> 写成 <code>manual</code> 可强制退回手改模式，该配置会被 secondmate home 继承。跨 home 交接安全由 <code>secondmate-provisioning</code> 技能与 <code>bin/fm-backlog-handoff.sh</code> 负责。</p><h2 id="_15-5-运维工具箱与日常巡检" tabindex="-1">15.5 运维工具箱与日常巡检 <a class="header-anchor" href="#_15-5-运维工具箱与日常巡检" aria-label="Permalink to &quot;15.5 运维工具箱与日常巡检&quot;">​</a></h2><p><code>docs/scripts.md</code> 是 <code>bin/</code> 工具带的权威索引，日常最常用的几个：</p><table tabindex="0"><thead><tr><th>脚本</th><th>用途</th></tr></thead><tbody><tr><td><code>fm-session-start.sh</code></td><td>会话启动摘要（第 2 章）</td></tr><tr><td><code>fm-bearings-board.sh</code> / <code>fm-bearings-snapshot.sh</code></td><td><code>/bearings</code> 的看板与快照支撑</td></tr><tr><td><code>fm-spawn.sh</code></td><td>启动 crewmate / secondmate（fail-closed 校验）</td></tr><tr><td><code>fm-crew-state.sh</code></td><td>深读某个任务的真实当前状态</td></tr><tr><td><code>fm-backlog-handoff.sh</code> / <code>fm-backlog-receive.sh</code></td><td>跨 home backlog 交接</td></tr><tr><td><code>fm-remote-home-seed.sh</code> / <code>fm-on.sh</code></td><td>远程路由供给与远程命令执行</td></tr><tr><td><code>fm-teardown.sh</code></td><td>带 landed-work 测试的安全收尾</td></tr><tr><td><code>fm-tool-update-check.sh</code></td><td>依赖工具的更新检查（清单在 <code>config/watched-tools.json</code>）</td></tr></tbody></table><p>日常巡检的推荐节奏：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 每天开工：一条命令看全舰队四段式摘要（第 12 章）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/bearings</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> include</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> PRs</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 疑似某任务卡住：深读单个任务的当前状态</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">bin/fm-crew-state.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">task-i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 收工前：知识清扫 + 记忆归档（第 12 章）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/stow</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><h2 id="_15-6-语音中继与-w3c-追踪上下文" tabindex="-1">15.6 语音中继与 W3C 追踪上下文 <a class="header-anchor" href="#_15-6-语音中继与-w3c-追踪上下文" aria-label="Permalink to &quot;15.6 语音中继与 W3C 追踪上下文&quot;">​</a></h2><p>远程舰队的两个近期增强，都属于「接入外部世界」的边缘能力。</p><h3 id="语音中继-voice-relay" tabindex="-1">语音中继（voice relay） <a class="header-anchor" href="#语音中继-voice-relay" aria-label="Permalink to &quot;语音中继（voice relay）&quot;">​</a></h3><p>官方提供了一套** spoken interface**：对着一个位于 firstmate 前面的语音 agent 说话，它从 firstmate 自有记录里回答状态问题；当你要它做真工作时，它会明说出来并把请求入队（走 <code>bin/fm-inbox.sh note</code>，与船长的带外捕获同一入口），而不是假装执行。</p><p>架构上笔记本只负责录音与放音，桌面机持模型对话：<code>fm-voice-client.py --(ssh)--&gt; fm-voice-relay.py --&gt; Amazon Nova Sonic 2</code>，两端共享一条 SSH exec 字节流（帧格式由 <code>bin/fm_voice_frame.py</code> 独家拥有）。实测中继自身开销低于测量噪声（首音中位数 1.17s vs 直连 1.23s）。边界也很明确：当前构建不支持打断语音中的 agent，也不支持跨问题上下文继承。</p><h3 id="w3c-追踪上下文-trace-context" tabindex="-1">W3C 追踪上下文（trace-context） <a class="header-anchor" href="#w3c-追踪上下文-trace-context" aria-label="Permalink to &quot;W3C 追踪上下文（trace-context）&quot;">​</a></h3><p>FirstMate 可以向每个 spawn 的 agent 传播 W3C <code>traceparent</code>，让外部观察者把每个任务识别为恰好一条 trace。特性默认关闭、源码自有、供应商中立：</p><ul><li>任务首次 spawn 时新铸一个 trace 根，relaunch 时从 meta 原样复用——<strong>一条 trace 归一个任务</strong>，永不合并无关任务、永不为同一任务铸第二个身份；</li><li>载体以 <code>TRACEPARENT</code> 环境变量在启动前注入（与 <code>GOTMPDIR</code> 同一注入点），并以 <code>traceparent=</code> 记入 <code>state/&lt;id&gt;.meta</code>——两者恒为同一字符串，观察者读元数据即可重建子进程收到的身份；</li><li>覆盖 ship/scout 的全部后端（tmux/herdr/zellij/orca/cmux）与除 muse 外的 secondmate 适配器；远程路由下由父方解析载体传给远程主机、远端校验后注入并回报实际值；</li><li>注意这是 FirstMate 约定而非标准：OpenTelemetry SDK 不会自动读环境变量，下游需显式读取 env 或 meta 字段。</li></ul><h2 id="_15-7-本章小结" tabindex="-1">15.7 本章小结 <a class="header-anchor" href="#_15-7-本章小结" aria-label="Permalink to &quot;15.7 本章小结&quot;">​</a></h2><ul><li>远程 secondmate = 整个 home 放上 SSH 主机：公钥认证、禁 agent forwarding、有界死链检测是三条红线；</li><li>远程入口只接受编码 argv 的 <code>fm-*.sh</code>，作业 worker 单作业串行、可抢占长轮询、随检出失效自我了断；</li><li>PATH 靠文件系统发现构建，shell 启动文件不参与；缺口由 doctor <code>--fix</code> 的自有 wrapper 补齐；</li><li>供给用 <code>fm-remote-home-seed.sh</code>，就绪检查「只读 → --fix → 再只读」，失败回滚（SSH 255 除外）；启动用 <code>fm-spawn.sh &lt;id&gt; --secondmate</code>，远程统一 Herdr <code>fm-remote</code> 空间；</li><li>恢复铁律：远程路由不可用绝不降级为本地替换；</li><li>Relay 默认惰性，token 即同意范围；承诺的公开回复是磁盘持久状态，只有绑定 home 能发帖，dry-run 先行；</li><li>backlog 只记工作项；决策是 <code>--kind captain</code> 的 hold；日常巡检靠 <code>/bearings</code>、<code>fm-crew-state.sh</code> 与 <code>/stow</code>。</li></ul>`);
      _push(ssrRenderComponent(_component_Quiz, { items: quiz }, null, _parent));
      _push(`<h2 id="🛠️-动手实践" tabindex="-1">🛠️ 动手实践 <a class="header-anchor" href="#🛠️-动手实践" aria-label="Permalink to &quot;🛠️ 动手实践&quot;">​</a></h2><ol><li>用一台云主机或家里闲置的 Mac/Linux 机器，完整走一遍 15.1–15.2 节：配置 SSH alias、克隆 firstmate、暴露 entrypoint、<code>fm-remote-home-seed.sh</code> 供给一个不带项目的远程路由，并截图记录 doctor 就绪检查的「红 → --fix → 绿」过程。</li><li>演练恢复铁律：启动远程 secondmate 后，在远程主机上 <code>kill</code> 掉其会话进程，观察主 home 重启后的 reconcile 行为；再临时断开 SSH（改错端口），确认主 home 报告「远程不可用」而不是在本地悄悄重建。</li><li>为你的 home 配置 Relay：在 <code>.env</code> 放入 pairing token，先用 dry-run preview 模式跑几天，检查本地记录的 would-be replies 内容是否都符合「可公开、可逆」标准，再决定是否 go-live；写一份你自己的公开回复安全清单。</li></ol></div>`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("firstmate/ch15.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
export {
  __pageData,
  _sfc_main as default
};
