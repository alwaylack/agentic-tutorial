import { useSSRContext, resolveComponent } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
const __pageData = JSON.parse('{"title":"第 7 章 · 实验性后端：herdr、zellij、Orca 与 cmux","description":"","frontmatter":{},"headers":[],"relativePath":"firstmate/ch07.md","filePath":"firstmate/ch07.md","lastUpdated":1787834555000}');
const __default__ = { name: "firstmate/ch07.md" };
const _sfc_main = /* @__PURE__ */ Object.assign(__default__, {
  __ssrInlineRender: true,
  setup(__props) {
    const quiz = [
      {
        question: "关于四个实验性后端的自动检测规则，正确的是？",
        options: [
          "四个后端都能被自动检测，无需任何设置",
          "只有 zellij 和 orca 能被自动检测",
          "herdr 与 cmux 可以被自动检测，zellij 与 orca 永不自动检测",
          "codex-app 已经是受支持的 runtime backend"
        ],
        answer: 2,
        explain: "官方规则明确：herdr（HERDR_ENV=1 且不在 tmux 内）和 cmux（CMUX_WORKSPACE_ID）参与运行时自动检测；zellij 与 orca 是 explicit-only，必须显式设置；codex-app 目前不是 runtime backend。"
      },
      {
        question: "cmux Password 模式下，适配器读取 config/cmux-socket-password 的行为是？",
        options: [
          "启动时读一次并缓存到内存",
          "每次调用都现读文件，且文件不存在时不覆盖环境中已有的 CMUX_SOCKET_PASSWORD",
          "把密码写回 cmux.json 以便应用记住",
          "密码错误时自动降级到 allowAll 模式"
        ],
        answer: 1,
        explain: "官方文档说明适配器每次调用都 fresh read 密码文件，文件缺失时不会覆盖操作者自己的环境变量密码；allowAll 是不推荐的开放模式，绝不是降级目标。"
      },
      {
        question: "zellij 的 CLI 动作命令对不存在的会话或 pane 也返回 exit 0，FirstMate 适配器如何应对？",
        options: [
          "信任 exit 0，因为它代表 zellij 已收到命令",
          "改用轮询等待 zellij 自己报错",
          "操作前做结构化校验（会话、pane、scoped title），操作后再校验响应形状，不把 exit 0 当成功",
          "放弃 zellij 后端，强制切回 tmux"
        ],
        answer: 2,
        explain: "由于退出码不可信，适配器在每个操作前后进行结构化验证，pane 在校验与操作之间消失时报告这个窄竞态，而不是误判成功；死 pane 的空屏幕分类为 unknown，永不当作确认。"
      },
      {
        question: "一个从未创建过 config/herdr-presentation-spaces 的 home，在 Herdr 0.7.5 上 spawn 新任务会发生什么？",
        options: [
          "默认开启 presentation space 投影",
          "spawn 直接失败并要求升级",
          "使用普通平铺布局，并针对该版本警告一次，提示升级到 0.8.0 可恢复投影",
          "随机选择投影或平铺布局"
        ],
        answer: 2,
        explain: "未配置的 home 只在达到 0.8.0 版本底线时才默认投影；低于底线使用普通布局并每版本警告一次（由 state/.herdr-presentation-floor-<release> 标记去重），升级或写 off 都是官方给出的出路。"
      }
    ];
    return (_ctx, _push, _parent, _attrs) => {
      const _component_Quiz = resolveComponent("Quiz");
      _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="第-7-章-·-实验性后端-herdr、zellij、orca-与-cmux" tabindex="-1">第 7 章 · 实验性后端：herdr、zellij、Orca 与 cmux <a class="header-anchor" href="#第-7-章-·-实验性后端-herdr、zellij、orca-与-cmux" aria-label="Permalink to &quot;第 7 章 · 实验性后端：herdr、zellij、Orca 与 cmux&quot;">​</a></h1><blockquote><p>本章目标：掌握 tmux 之外的四个实验性会话后端的选择规则、安装要点与安全边界，学会为不同工作环境挑选合适的后端并正确切换。</p></blockquote><h2 id="_7-1-为什么需要多个后端" tabindex="-1">7.1 为什么需要多个后端 <a class="header-anchor" href="#_7-1-为什么需要多个后端" aria-label="Permalink to &quot;7.1 为什么需要多个后端&quot;">​</a></h2><p>第 6 章我们学习了 tmux 参考后端——它是 FirstMate 唯一的&quot;参考实现&quot;（reference backend），行为保证最完整。但并非所有人都用 tmux：有人习惯 Herdr 这类新一代终端管理器，有人在 macOS 上把 cmux 当主力终端，还有人已经深度使用 zellij 或 Orca。</p><p>FirstMate 的答案是：<strong>会话端点可以替换，监督协议不变</strong>。无论 crewmate 跑在哪个后端的窗口里，firstmate 都通过统一的元数据路由接口（<code>bin/fm-peek.sh &lt;id&gt;</code> 查看、<code>FM_HOME=&lt;home&gt; bin/fm-send.sh &lt;id&gt; &#39;&lt;text&gt;&#39;</code> 发消息）来监督它——你不需要为每个后端学一套新指令。</p><p>不过官方对这四个后端的定位很明确：<strong>它们都是实验性的</strong>（experimental）。tmux 拥有最完整的维护者验证（见 <code>docs/verification/runtime-backends.md</code>），其余后端各有已记录的能力边界。选择时的优先级建议是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tmux        参考默认，保证最完整，无特殊理由就选它</span></span>
<span class="line"><span>herdr       想要原生 busy/idle/blocked 状态信号时选它</span></span>
<span class="line"><span>zellij      已经在用 zellij 且接受其当前限制时选它</span></span>
<span class="line"><span>orca        macOS 用户想连 worktree 一起交给 Orca 管理时选它</span></span>
<span class="line"><span>cmux        macOS GUI 用户想把任务放进 cmux 侧边栏时选它</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h2 id="_7-2-选择规则-显式设置与自动检测" tabindex="-1">7.2 选择规则：显式设置与自动检测 <a class="header-anchor" href="#_7-2-选择规则-显式设置与自动检测" aria-label="Permalink to &quot;7.2 选择规则：显式设置与自动检测&quot;">​</a></h2><p>所有后端的共享选择语义由 <code>docs/configuration.md</code> 统一拥有。指定后端有三种方式：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式一：本地 gitignored 配置文件（持久生效）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">echo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;zellij&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> config/backend</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式二：环境变量（仅本次启动生效）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">FM_BACKEND</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">zellij</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> claude</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式三：对话中直接向 firstmate 提出明确请求</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>关键区别在于<strong>谁能被自动检测</strong>（runtime auto-detection）：</p><table tabindex="0"><thead><tr><th>后端</th><th>自动检测</th><th>说明</th></tr></thead><tbody><tr><td>tmux</td><td>✅</td><td>参考默认</td></tr><tr><td>herdr</td><td>✅</td><td>主会话原生跑在 <code>HERDR_ENV=1</code> 下且不在 tmux 内时选中</td></tr><tr><td>cmux</td><td>✅</td><td>主会话跑在 cmux 内时选中</td></tr><tr><td>zellij</td><td>❌</td><td><strong>永不</strong>自动检测，必须显式设置</td></tr><tr><td>orca</td><td>❌</td><td><strong>永不</strong>自动检测，必须显式设置</td></tr><tr><td>codex-app</td><td>—</td><td>尚不是 runtime backend（blocked boundary，见 <code>docs/codex-app-backend.md</code>）</td></tr></tbody></table><p>自动检测遵循&quot;<strong>内层多路复用器胜出</strong>&quot;原则。以 cmux 为例，检测顺序是先查 tmux，再查 herdr，最后才是 cmux——所以嵌套在 cmux 里的 tmux pane 会被识别为 tmux 后端。herdr 同理：tmux pane 嵌套在 Herdr 内时解析为 tmux。</p><p>还有一个重要细节：<strong>自动检测只决定后端，永不授予凭据或放行安全设置</strong>。比如 cmux 的套接字访问没配置好，自动检测也不会帮你绕过——spawn 会带着可操作的设置指引拒绝启动。</p><h2 id="_7-3-herdr-带原生状态信号的终端管理器" tabindex="-1">7.3 herdr：带原生状态信号的终端管理器 <a class="header-anchor" href="#_7-3-herdr-带原生状态信号的终端管理器" aria-label="Permalink to &quot;7.3 herdr：带原生状态信号的终端管理器&quot;">​</a></h2><p>herdr 的最大卖点是提供<strong>原生的 busy / idle / blocked 状态信号</strong>——其他后端没有这种信号，监督只能靠屏幕截图哈希轮询加各 harness 自己的生命周期语义来判断忙碌状态。</p><p>基础要求：herdr、<code>jq</code>，以及协议版本底线。它的拓扑约定是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Herdr 会话</span></span>
<span class="line"><span>└── home 工作区（home label: firstmate 或 2ndmate-&lt;id&gt;）</span></span>
<span class="line"><span>    ├── fm-&lt;task-id-1&gt;  任务 tab</span></span>
<span class="line"><span>    ├── fm-&lt;task-id-2&gt;  任务 tab</span></span>
<span class="line"><span>    └── ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>每个任务 tab 放在与发起者完全一致的工作区里（从注入的 socket 身份现场解析，而不是信任快照）。身份无法精确解析时 spawn 直接拒绝，绝不退化为按标签搜索——因为 Herdr <strong>不强制工作区/tab 标签唯一</strong>，标签不能决定工人去向。</p><h3 id="presentation-spaces-与版本底线" tabindex="-1">Presentation spaces 与版本底线 <a class="header-anchor" href="#presentation-spaces-与版本底线" aria-label="Permalink to &quot;Presentation spaces 与版本底线&quot;">​</a></h3><p>Herdr 0.8.0 起，每个新 crewmate/scout 默认被投影到一个<strong>一次性的单任务工作区</strong>（presentation space），让视觉上每任务一栏。但这有一个版本底线：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 关闭投影（写入本地 gitignored 配置）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">echo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;off&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> config/herdr-presentation-spaces</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 强制开启（即使低于版本底线也接受已文档化的焦点移动代价）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">echo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;on&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> config/herdr-presentation-spaces</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 文件不存在 → 由版本底线决定：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   Herdr &gt;= 0.8.0 → 默认开启投影</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   Herdr &lt;  0.8.0 → 使用普通的按 home 平铺布局，并警告一次</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>为什么有这条底线？0.8.0 之前的 Herdr 有一个焦点缺陷：清理任务时活动工作区会被短暂移走再恢复（约七分之一秒），而&quot;每次清理都清空整个工作区&quot;恰好是唯一能避开该缺陷的删除形态。0.8.0 修复了这个问题，所以未配置的家只在达标版本上才默认开投影。</p><p>仓库还自带两个辅助脚本 <code>bin/backends/herdr-workspace-move.py</code> 和 <code>bin/backends/herdr-eventwait.py</code>，分别处理工作区移动和事件等待。</p><h2 id="_7-4-zellij-显式专用的共享会话后端" tabindex="-1">7.4 zellij：显式专用的共享会话后端 <a class="header-anchor" href="#_7-4-zellij-显式专用的共享会话后端" aria-label="Permalink to &quot;7.4 zellij：显式专用的共享会话后端&quot;">​</a></h2><p>zellij 是<strong>显式专用</strong>（explicit-only）后端：永远不会被自动检测到。它使用一个共享会话（默认名 <code>firstmate</code>），每个任务一个 tab：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 用 FM_ZELLIJ_SESSION 为隔离验证选择别的会话名</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">FM_ZELLIJ_SESSION</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">fm-test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> zellij</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --session</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fm-test</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 附上去围观（日常监督不需要 attach）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">zellij</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> attach</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> firstmate</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>要求 zellij 0.44+ 和 <code>jq</code>。可见标题采用 home 作用域格式 <code>fm-&lt;home-label&gt;-&lt;id&gt;</code>（如 <code>fm-firstmate-42</code>），加上 FirstMate 安装路径的稳定短哈希——这样主家、secondmate 家、甚至同一台机器上的多个 FirstMate 安装共用一个会话时，任务也不会互相混淆。</p><p>zellij 有个著名的坑：<strong>CLI 动作命令对不存在的会话或 pane 也返回 exit 0</strong>。适配器的对策是不信任退出码，而是在每次操作前后做结构化校验：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>操作前：验证会话存在 → 验证 terminal pane 存在 → 验证期望的 scoped title</span></span>
<span class="line"><span>操作后：校验 JSON/整数响应形状</span></span>
<span class="line"><span>残余竞态：pane 可能在校验后、操作前消失 → 下游报告这个窄竞态，</span></span>
<span class="line"><span>         而不是把 exit 0 当成功</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>发送消息也有类似严谨性：Enter 发出前，适配器必须证明 composer 内容恰好增加了粘贴的文本；不可读的 composer、粘贴落到别处、无关输出都会失败而不提交。死 pane 更是永远 fail-safe——空屏幕分类为 <code>unknown</code>，绝不会被误认为&quot;确认送达&quot;。</p><h2 id="_7-5-orca-worktree-与终端一体化的-macos-后端" tabindex="-1">7.5 Orca：worktree 与终端一体化的 macOS 后端 <a class="header-anchor" href="#_7-5-orca-worktree-与终端一体化的-macos-后端" aria-label="Permalink to &quot;7.5 Orca：worktree 与终端一体化的 macOS 后端&quot;">​</a></h2><p>Orca 是四个实验后端中最特别的一个：<strong>Orca app 同时拥有任务的 git worktree 和终端端点</strong>。其他后端都是&quot;Treehouse 提供 worktree + 多路复用器提供会话&quot;的两层结构，Orca 把两层合并了：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tmux/herdr/zellij/cmux 任务：Treehouse worktree + 会话端点（两层）</span></span>
<span class="line"><span>orca 任务：                  Orca-managed worktree + Orca 终端（一层）</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>因此 <code>fm-spawn.sh</code> 对 orca 任务<strong>不调用 Treehouse</strong>。spawn 前 firstmate 要求 <code>orca status --json</code> 报告 <code>reachable=true</code> 且 <code>state=&quot;ready&quot;</code>；项目的第一个任务会自动用 <code>orca repo add --path</code> 注册仓库，无需手工注册。</p><p>要点清单：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>平台          仅 macOS</span></span>
<span class="line"><span>CLI 安装      brew install orca</span></span>
<span class="line"><span>选择方式      config/backend 写 orca / FM_BACKEND=orca / 明确请求（永不自动检测）</span></span>
<span class="line"><span>secondmate    不支持 secondmate spawn</span></span>
<span class="line"><span>按键支持      Enter、Ctrl-C 支持；Escape 不支持</span></span>
<span class="line"><span>隔离规则      正常的隔离与 unlanded-work 拒绝规则照常适用</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>清理时保留全部共享安全检查：scout 仍然必须先交出报告并完成 decision inventory 才能收尾。</p><h2 id="_7-6-cmux-macos-gui-工作区后端" tabindex="-1">7.6 cmux：macOS GUI 工作区后端 <a class="header-anchor" href="#_7-6-cmux-macos-gui-工作区后端" aria-label="Permalink to &quot;7.6 cmux：macOS GUI 工作区后端&quot;">​</a></h2><p>cmux 同样是 macOS 专属、GUI 优先的后端，任务以 workspace + surface 形态出现在 cmux 侧边栏里。它不适合 headless 或纯 SSH 环境。</p><h3 id="套接字访问-一次性手动设置" tabindex="-1">套接字访问：一次性手动设置 <a class="header-anchor" href="#套接字访问-一次性手动设置" aria-label="Permalink to &quot;套接字访问：一次性手动设置&quot;">​</a></h3><p>cmux 默认的控制模式拒绝外部 shell 连接，而 FirstMate 必须从外部进程控制它。首次 spawn 前需要在 cmux 的 Settings &gt; Automation 里选一个可行的 Socket Control Mode：</p><table tabindex="0"><thead><tr><th>设置</th><th>值</th><th>FirstMate 支持</th><th>安全边界</th></tr></thead><tbody><tr><td>Off</td><td><code>off</code></td><td>❌</td><td>套接字监听关闭</td></tr><tr><td>cmux processes only</td><td><code>cmuxOnly</code></td><td>❌</td><td>仅 cmux 应用后代可连</td></tr><tr><td>Automation mode</td><td><code>automation</code></td><td>✅ 推荐</td><td>属主 0600 套接字，限当前 macOS 用户进程</td></tr><tr><td>Password mode</td><td><code>password</code></td><td>✅</td><td>0600 套接字 + 认证握手</td></tr><tr><td>Full open access</td><td><code>allowAll</code></td><td>✅ 不推荐</td><td>0666 世界可写，任何本地用户无需认证即可执行命令</td></tr></tbody></table><p>Password 模式下密码存放有两种途径：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 途径一：本地 gitignored 文件的第一行</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">echo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;your-secret&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> config/cmux-socket-password</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 途径二：环境变量 CMUX_SOCKET_PASSWORD</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>适配器<strong>每次调用都现读</strong>密码文件，并且文件不存在时绝不会覆盖你环境中已有的 <code>CMUX_SOCKET_PASSWORD</code>。注意：模式和密码都要通过 cmux UI 配置，不要手改 <code>cmux.json</code>——应用不保留手工添加的密码键。</p><h3 id="uuid-与恢复" tabindex="-1">UUID 与恢复 <a class="header-anchor" href="#uuid-与恢复" aria-label="Permalink to &quot;UUID 与恢复&quot;">​</a></h3><p>每个任务拥有一个 workspace + 一个 surface，元数据形如：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>backend=cmux</span></span>
<span class="line"><span>window=&lt;workspace-uuid&gt;:&lt;surface-uuid&gt;</span></span>
<span class="line"><span>cmux_workspace_id=&lt;workspace-uuid&gt;</span></span>
<span class="line"><span>cmux_surface_id=&lt;surface-uuid&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><strong>workspace UUID 在应用重启后会变</strong>，所以恢复逻辑按作用域标题搜索再解析当前 surface id，UUID 从不作为恢复权威。运行时检测标记是 <code>CMUX_WORKSPACE_ID</code>（<code>CMUX_SOCKET_PATH</code> 不算数，因为可能在 cmux 外设置）。</p><h3 id="边界-codex-app-为什么还不是后端" tabindex="-1">边界：Codex App 为什么还不是后端 <a class="header-anchor" href="#边界-codex-app-为什么还不是后端" aria-label="Permalink to &quot;边界：Codex App 为什么还不是后端&quot;">​</a></h3><p>官方文档专门用一篇 <code>docs/codex-app-backend.md</code> 说明一个“负边界”：<strong>Codex App 不是可选的 runtime backend</strong>。Codex Desktop 的宿主工具能创建并监督可见线程，但 Firstmate 的后端脚本都是 shell 入口，无法调用这些宿主工具——缺少的是 Codex Desktop 官方支持的 shell 可调用传输（CLI 封装、文档化 JSON-RPC/MCP 或维护良好的 helper），而不是另一个本地账本。</p><p>未来若要成为真正的后端，必须满足与终端后端相同的生命周期契约：创建任务端点并返回持久线程 id → 发送指令与后续消息 → 读取足够状态以监督 → 归档/终止同端点 → <strong>线程必须回写 <code>state/&lt;id&gt;.status</code> 生命周期行</strong>。状态回传通道是强制的：一个无法向 Firstmate 汇报的可见线程不是完整后端。在官方桥接落地前，<code>codex-app</code> 不会进入已知或可 spawn 的后端注册表——选型时不要被“能跑起来”的演示迷惑。</p><h2 id="_7-7-切换后端与验证" tabindex="-1">7.7 切换后端与验证 <a class="header-anchor" href="#_7-7-切换后端与验证" aria-label="Permalink to &quot;7.7 切换后端与验证&quot;">​</a></h2><p>无论选哪个后端，日常监督接口完全一致：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">bin/fm-peek.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                              # 查看 crewmate 屏幕</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">FM_HOME</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">home</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> bin/fm-send.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;steer text&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # 发送转向指令</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>验证后端是否生效，方法是 spawn 一个小任务并检查任务元数据中的 <code>backend=</code> 字段及对应后端的专属字段：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tmux     → backend=tmux     window=&lt;session&gt;:&lt;window&gt;</span></span>
<span class="line"><span>herdr    → backend=herdr    （tab/workspace 标识）</span></span>
<span class="line"><span>zellij   → backend=zellij   zellij_session= zellij_tab_id= zellij_pane_id=</span></span>
<span class="line"><span>orca     → backend=orca     orca_worktree_id= worktree=</span></span>
<span class="line"><span>cmux     → backend=cmux     cmux_workspace_id= cmux_surface_id=</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>每个后端的活跃维护者验证（源码与实测证据，包括套接字模式、焦点行为等）统一记录在 <code>docs/verification/runtime-backends.md</code>。切换后端前的检查单：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>□ 新后端的 CLI 与 jq 已安装且版本达标</span></span>
<span class="line"><span>□ 平台匹配（orca/cmux 仅 macOS）</span></span>
<span class="line"><span>□ 特殊设置已完成（cmux 套接字模式 / Orca app 就绪）</span></span>
<span class="line"><span>□ config/backend 或 FM_BACKEND 写入正确的后端名</span></span>
<span class="line"><span>□ spawn 小任务验证 backend= 元数据字段</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><ul><li>四个实验后端（herdr/zellij/orca/cmux）各有取舍，tmux 始终是行为保证最完整的参考默认；</li><li>自动检测只覆盖 herdr 与 cmux，zellij 与 orca 永远必须显式设置，codex-app 还不是 runtime backend；</li><li>检测顺序 tmux → herdr → cmux 保证&quot;内层多路复用器胜出&quot;，且自动检测永不授予凭据或放行安全设置；</li><li>各后端的安全边界要提前配好：cmux 套接字模式与密码、herdr presentation spaces 版本底线、zellij 结构化校验弥补 exit 0 陷阱；</li><li>无论后端如何切换，监督接口 <code>fm-peek.sh</code> / <code>fm-send.sh</code> 与元数据路由保持不变。</li></ul>`);
      _push(ssrRenderComponent(_component_Quiz, { items: quiz }, null, _parent));
      _push(`<h2 id="🛠️-动手实践" tabindex="-1">🛠️ 动手实践 <a class="header-anchor" href="#🛠️-动手实践" aria-label="Permalink to &quot;🛠️ 动手实践&quot;">​</a></h2><ol><li>对照 7.2 的表格检查你的环境：你正在用的终端复用器属于哪一类？如果它是 zellij 或 orca，写出让它生效的两种显式设置方式，并解释为什么这两个后端不能依赖自动检测。</li><li>在 macOS 上安装 cmux 并完成 Socket Control Mode 设置（推荐 automation 模式）：先故意保持默认的 cmuxOnly 模式尝试 spawn，观察拒绝信息中的设置指引，再改为 automation 模式重试，最后 spawn 小任务验证元数据中出现 <code>cmux_workspace_id</code> 与 <code>cmux_surface_id</code>。</li><li>你的 Herdr 是 0.7.x 版本：分别实验&quot;不创建配置文件&quot;、&quot;写入 off&quot;、&quot;写入 on&quot;三种情况下 spawn 新任务的工作区布局差异，观察低于 0.8.0 版本底线时的一次性警告内容，并用 <code>state/.herdr-presentation-floor-&lt;release&gt;</code> 标记文件解释为什么删掉标记后警告会再次出现。</li></ol></div>`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("firstmate/ch07.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
export {
  __pageData,
  _sfc_main as default
};
