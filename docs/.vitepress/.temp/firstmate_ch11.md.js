import { useSSRContext, resolveComponent } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
const __pageData = JSON.parse('{"title":"第 11 章 · 零 token 事件驱动监督：watcher 与 turn-end guard","description":"","frontmatter":{},"headers":[],"relativePath":"firstmate/ch11.md","filePath":"firstmate/ch11.md","lastUpdated":1787834555000}');
const __default__ = { name: "firstmate/ch11.md" };
const _sfc_main = /* @__PURE__ */ Object.assign(__default__, {
  __ssrInlineRender: true,
  setup(__props) {
    const quiz = [
      {
        question: "FirstMate 的 watcher 被称为「零 token」，根本原因是？",
        options: [
          "它使用了一个参数量为零的特殊 LLM",
          "它是纯 bash 脚本，分类唤醒事件不需要任何 LLM 调用",
          "token 费用由 GitHub Actions 免费额度承担",
          "它把请求转发给了免费的公共 API"
        ],
        answer: 1,
        explain: "官方描述明确指出 watcher sleeps on the fleet and classifies detected wakes in bash——分类工作完全在 bash 中完成，只有确认可行动事件时才消耗 token 唤醒 firstmate。"
      },
      {
        question: "可行动唤醒事件为什么先写入 state/.wake-queue 而不是直接交给 firstmate？",
        options: [
          "为了让日志更美观",
          "为了持久化：watcher 或处理回合被打断后可从磁盘恢复，不丢失队列记录",
          "为了节省 tmux 窗口数量",
          "因为 LLM 无法直接读取内存中的数据"
        ],
        answer: 1,
        explain: "官方架构文档说明队列只在生成恢复证据后写入，interrupted watcher 或 handling turn 可以在不丢失队列记录的前提下恢复。"
      },
      {
        question: "turn-end guard 的不变量是什么？",
        options: [
          "任何回合结束时都必须重启 watcher",
          "当有工作需要监督而没有身份匹配的 watcher 持新鲜信标时，必须阻止盲停或强制一次有界跟进",
          "每回合最多只能派发一个 crewmate 任务",
          "watcher 必须每 300 秒唤醒一次 firstmate"
        ],
        answer: 1,
        explain: '"no turn ends blind" 是 guard 的核心承诺：在回合边界检测到监督需求未被满足时，harness 集成必须阻塞或发起一次 bounded follow-up，绝不允许无人监督地静默结束。'
      },
      {
        question: "关于 restart-proof 的恢复行为，正确的说法是？",
        options: [
          "所有存活性不确定的 agent 一律重启以保证干净状态",
          "确认死亡的 secondmate endpoint 通过相同 spawn 路径重启，存疑的保留不动以避免重复监督者",
          "重启后所有任务从头开始执行",
          "只有 tmux 后端支持重启恢复"
        ],
        answer: 1,
        explain: "官方文档规定：confirmed-dead endpoints are closed and relaunched through the same secondmate spawn path, while ambiguous liveness reads are left untouched to avoid duplicate supervisors。"
      }
    ];
    return (_ctx, _push, _parent, _attrs) => {
      const _component_Quiz = resolveComponent("Quiz");
      _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="第-11-章-·-零-token-事件驱动监督-watcher-与-turn-end-guard" tabindex="-1">第 11 章 · 零 token 事件驱动监督：watcher 与 turn-end guard <a class="header-anchor" href="#第-11-章-·-零-token-事件驱动监督-watcher-与-turn-end-guard" aria-label="Permalink to &quot;第 11 章 · 零 token 事件驱动监督：watcher 与 turn-end guard&quot;">​</a></h1><blockquote><p>本章目标：理解 FirstMate 如何用「零 token 事件驱动监督」替代人工盯梢——bash watcher 睡眠待命、turn-end guard 兜底盲停、重启后自动 reconcile 接续。</p></blockquote><h2 id="_11-1-为什么需要「零-token」监督" tabindex="-1">11.1 为什么需要「零 token」监督 <a class="header-anchor" href="#_11-1-为什么需要「零-token」监督" aria-label="Permalink to &quot;11.1 为什么需要「零 token」监督&quot;">​</a></h2><p>想象一个常见场景：你给船队下达了三个并行任务，然后切去干别的事。此时谁在盯着这些任务？</p><p>传统的做法是<strong>轮询</strong>：每隔几分钟问一次 agent「任务完成了吗？」。这种方式有两个致命缺陷：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>轮询模式的代价：</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. token 浪费：每次询问都要消耗一次 LLM 调用，</span></span>
<span class="line"><span>   即使绝大多数时候回答是&quot;还在跑&quot;</span></span>
<span class="line"><span>2. 反应迟钝：两次询问之间发生的异常只能干等被发现</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>FirstMate 把这个模型整个翻转过来。官方架构文档的描述是：</p><blockquote><p>A zero-token bash watcher sleeps on the fleet, classifies detected wakes in bash, and wakes the first mate only when something is actionable.</p><p>（一个零 token 的 bash watcher 睡眠于舰队之上，在 bash 中对唤醒事件分类，只在确有可行动事件时才唤醒第一副手。）</p></blockquote><p>关键词是 <strong>zero-token</strong>：watcher 本身是一个纯 bash 脚本，不消耗任何 LLM 调用。它持续观察会话后端（tmux 窗口等）的状态信号，把「值得打扰船长的事件」和「良性噪音」区分开，只有前者才会触发 firstmate 的一次真实回合。</p><h2 id="_11-2-watcher-的工作机制-睡眠、分类、唤醒" tabindex="-1">11.2 watcher 的工作机制：睡眠、分类、唤醒 <a class="header-anchor" href="#_11-2-watcher-的工作机制-睡眠、分类、唤醒" aria-label="Permalink to &quot;11.2 watcher 的工作机制：睡眠、分类、唤醒&quot;">​</a></h2><p>核心脚本是 <code>bin/fm-watch.sh</code>。它的循环逻辑可以概括为三步：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌─────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│  1. SLEEP    睡眠于舰队之上，监听状态信号          │</span></span>
<span class="line"><span>│  2. CLASSIFY 在 bash 中分类唤醒事件               │</span></span>
<span class="line"><span>│     ├─ actionable → 写入队列并退出，唤醒 firstmate │</span></span>
<span class="line"><span>│     └─ benign     → 吸收掉，继续睡                │</span></span>
<span class="line"><span>│  3. WAKE     firstmate 被唤起处理积压的可行动事件   │</span></span>
<span class="line"><span>└─────────────────────────────────────────────────┘</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>什么算「可行动事件」（actionable）？官方文档列举了主要类别：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>可行动唤醒（actionable wakes）包括：</span></span>
<span class="line"><span>- captain 相关的状态信号</span></span>
<span class="line"><span>- 无动词信号且无法证明船员仍在工作</span></span>
<span class="line"><span>- 已认证的检查输出（如 PR merge 轮询、Relay mention）</span></span>
<span class="line"><span>- 船员无法证明在工作时的 stale pane</span></span>
<span class="line"><span>- 声明中的外部等待超时未解除</span></span>
<span class="line"><span>- heartbeat backstop 命中</span></span>
<span class="line"><span></span></span>
<span class="line"><span>良性噪音（benign）则被吸收：</span></span>
<span class="line"><span>- routine watcher polling（例行轮询）</span></span>
<span class="line"><span>- supervision no-ops（监督空操作）</span></span>
<span class="line"><span>- absorbed benign wakes（被吸收的良性唤醒）</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>关键工程细节是<strong>持久化唤醒队列</strong>。可行动事件不会直接丢进对话，而是先写入磁盘上的 <code>state/.wake-queue</code>：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>state/</span></span>
<span class="line"><span>├── .wake-queue          # 可行动事件的持久化队列</span></span>
<span class="line"><span>├── .watch-triage.log    # 被吸收的良性唤醒调试日志</span></span>
<span class="line"><span>└── .watch-cycle-exits.log  # watcher 生命周期记录</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>这样设计的好处是：即使 watcher 或处理回合中途被打断，队列记录也不会丢失，下一次可以从磁盘恢复继续处理。此外，每次向船长展示事件时都会附带一个 fleet-wide 的 OPEN DECISIONS 区块，保证早前被后续日志「淹没」的未决决策会持续浮出水面，直到被显式解决。</p><h2 id="_11-3-各-primary-harness-的接入方式" tabindex="-1">11.3 各 primary harness 的接入方式 <a class="header-anchor" href="#_11-3-各-primary-harness-的接入方式" aria-label="Permalink to &quot;11.3 各 primary harness 的接入方式&quot;">​</a></h2><p>watcher 需要 primary harness 配合才能「唤醒」firstmate——不同 harness 提供的钩子机制不同。FirstMate 为每种已验证的主 harness 都准备了适配：</p><table tabindex="0"><thead><tr><th>Harness</th><th>接入机制</th></tr></thead><tbody><tr><td>Claude Code</td><td>tracked <code>Stop</code> hook（<code>asyncRewake</code>），tokenless re-arm 循环</td></tr><tr><td>Grok</td><td>background-notify 唤醒周期</td></tr><tr><td>Pi / pi-signed</td><td>tracked primary watcher 扩展（<code>.pi/extensions/*.ts</code>）</td></tr><tr><td>Cursor Agent CLI</td><td>项目级 <code>.cursor/hooks.json</code> 的 <code>stop</code> hook，停靠于 watcher</td></tr><tr><td>Codex</td><td>bounded foreground checkpoints（有界前台检查点）</td></tr><tr><td>OpenCode</td><td>TUI plugin</td></tr></tbody></table><p>以 Claude Code 为例，它在 <code>.claude/settings.json</code> 中注册两个 <code>Stop</code> hook：</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;hooks&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;Stop&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">        &quot;hooks&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">          { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;type&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;command&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;command&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;bin/fm-turnend-guard.sh --claude&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">          {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">            &quot;type&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;command&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">            &quot;command&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;bin/fm-claude-stop-autoarm.sh&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">            &quot;asyncRewake&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">            &quot;timeout&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">28800</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">          }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p>第一个 hook 是 turn-end guard（下一节详讲）；第二个 <code>asyncRewake</code> 负责 tokenless re-arm——它让 Claude 会话在不消耗模型调用的情况下重新武装 watcher，形成「工作→空闲→再武装」的无缝循环。</p><p>Pi 的方式不同：<code>.pi/extensions/fm-primary-pi-watch.ts</code> 会在每次可行动唤醒发生时主动拆掉当前 watcher，再由自己拉起新的替换进程。因此 Pi 模式下「活着的、身份匹配的 watcher」才是健康常态。</p><p>对于未知 harness，FirstMate 提供 fallback 协议：按最保守的策略处理监督连续性，宁可多提醒也不静默失守。</p><h2 id="_11-4-turn-end-guard-不让任何回合盲目结束" tabindex="-1">11.4 turn-end guard：不让任何回合盲目结束 <a class="header-anchor" href="#_11-4-turn-end-guard-不让任何回合盲目结束" aria-label="Permalink to &quot;11.4 turn-end guard：不让任何回合盲目结束&quot;">​</a></h2><p>watcher 再可靠也有空窗期——比如刚处理完一轮、新 watcher 还没武装好的瞬间。如果恰好在这个瞬间 primary 结束了自己的回合，而舰队里还有工作进行中，会发生什么？没人监督了。</p><p>turn-end guard 就是堵住这个缺口的兜底机制，其不变量（invariant）写在官方文档里：</p><blockquote><p>When work, a process-event source, or Relay polling needs supervision at that boundary and no identity-matched watcher has a fresh beacon, the harness integration must either block the turn end or force one bounded follow-up.</p><p>（当边界时刻存在需要监督的工作、过程事件源或 Relay 轮询，而没有任何身份匹配的 watcher 持有新鲜信标时，harness 集成必须阻止该回合结束，或强制发起一次有界的跟进。）</p></blockquote><p>判定谓词位于 <code>bin/fm-turnend-guard.sh</code>，其检查链条如下：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 是否处于 FirstMate 主 home 范围内？</span></span>
<span class="line"><span>   ├─ 否 → 静默退出（普通项目不受影响）</span></span>
<span class="line"><span>   └─ 是 ↓</span></span>
<span class="line"><span>2. 是否有在途工作？（state/*.meta 计数 + process-event 源）</span></span>
<span class="line"><span>   ├─ 无 → 静默退出</span></span>
<span class="line"><span>   └─ 有 ↓</span></span>
<span class="line"><span>3. 是否有身份匹配的 watcher 且信标新鲜？</span></span>
<span class="line"><span>   ├─ 是 → 放行</span></span>
<span class="line"><span>   └─ 否 → 阻止盲停或强制一次有界跟进</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>注意「身份匹配」（identity-matched）这个限定词：guard 使用 PID 级别的严格检查——一个陈旧的信标即使还有活的 watcher 进程也会告警；反之一个新鲜但遗留的信标，如果锁缺失、进程已死或身份不匹配，同样会告警。信标新鲜度的宽限期由环境变量 <code>FM_GUARD_GRACE</code> 控制，默认 300 秒。</p><p>这套机制的精妙之处在于它与 auto-arm <strong>协作而非互斥</strong>：在回合边界上，auto-arm 正在为即将到来的空闲期拉起新 watcher，guard 会配合这个过程，而不是把过渡期的正常状态误判为故障。</p><h2 id="_11-5-restart-proof-杀掉会话也丢不了进度" tabindex="-1">11.5 restart-proof：杀掉会话也丢不了进度 <a class="header-anchor" href="#_11-5-restart-proof-杀掉会话也丢不了进度" aria-label="Permalink to &quot;11.5 restart-proof：杀掉会话也丢不了进度&quot;">​</a></h2><p>监督体系的最后一道保障是<strong>重启无恙</strong>（restart-proof）。FirstMate 的所有状态都活在持久化介质上：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Fleet 状态存放位置：</span></span>
<span class="line"><span>├── 会话后端本身        tmux（硬默认）/ herdr / cmux 等</span></span>
<span class="line"><span>├── no-mistakes 运行记录  任务执行轨迹</span></span>
<span class="line"><span>├── 状态事件日志         append-only status event logs</span></span>
<span class="line"><span>├── data/ 下的本地 markdown</span></span>
<span class="line"><span>│   ├── captain.md       船长偏好与指令</span></span>
<span class="line"><span>│   ├── captain-shared.md 共享操作记忆</span></span>
<span class="line"><span>│   └── learnings.md     学到的经验教训</span></span>
<span class="line"><span>└── 持久 secondmate homes</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>这意味着你可以随时 kill 掉 primary 会话——下次启动时，session start 会执行 reconcile：</p><div class="language-sh vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 强行结束 primary 会话（模拟崩溃或主动重启）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">kill</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">primary-session-pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 重新进入 firstmate 目录启动新会话</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> firstmate</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">claude</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 新会话从磁盘状态恢复现场，接续未完成的工作</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>恢复行为有明确的边界规则：确认死亡（confirmed-dead）的 secondmate agent endpoint 会被关闭并通过相同的 spawn 路径重启；而<strong>存活性不确定的读取会被原样保留</strong>，避免产生重复的监督者。对于 herdr 后端，服务器恢复布局后被确认无 agent 或已死的任务标签壳会被关闭替换，无需手工清理标签页。</p><p>官方建议的一个配套习惯是：在<strong>主动重置</strong>之前先运行 <code>/stow</code>（第 12 章详讲），把可能只存在于对话中的持久知识落盘。之后的新会话就能 reconcile 并继续前进。</p><h2 id="_11-6-控制平面与-pretooluse-守卫" tabindex="-1">11.6 控制平面与 PreToolUse 守卫 <a class="header-anchor" href="#_11-6-控制平面与-pretooluse-守卫" aria-label="Permalink to &quot;11.6 控制平面与 PreToolUse 守卫&quot;">​</a></h2><p>watcher 解决「何时唤醒」，本节补充「如何安全地干预」与「如何防止绕过监督」两块拼图。</p><h3 id="控制平面-fm-control-sh" tabindex="-1">控制平面：fm-control.sh <a class="header-anchor" href="#控制平面-fm-control-sh" aria-label="Permalink to &quot;控制平面：fm-control.sh&quot;">​</a></h3><p>FirstMate 与运行中的 agent 有两条不等价的通道：<strong>数据平面</strong> <code>bin/fm-send.sh</code> 传对话文本（对 secondmate 自动加路由标记）；<strong>控制平面</strong> <code>bin/fm-control.sh</code> 传生命周期动词。拆分的原因：带路由标记的 <code>/quit</code> 会被当成普通聊天被模型「思考」而不是执行。</p><p>控制平面只接受白名单动词，没有任意文本入口：</p><table tabindex="0"><thead><tr><th>动词</th><th>效果</th><th>后置条件</th></tr></thead><tbody><tr><td><code>interrupt</code></td><td>投递 harness 验证过的中断序列，agent 保持运行</td><td>仅适配器自证取消才报 <code>cancel=confirmed</code>，否则 <code>unconfirmed</code></td></tr><tr><td><code>exit</code></td><td>停止 agent，保留端点、worktree 与全部未提交修改</td><td>后端分类器确认 agent 已消失；已停止视为幂等成功</td></tr><tr><td><code>relaunch</code></td><td>同端点同 worktree 换新 agent（可显式指定 harness/model/effort）</td><td>新 agent 存活且持久记录写入实际运行的 harness</td></tr></tbody></table><p>注意边界：<strong>teardown/discard 不是动词</strong>（归 <code>fm-teardown.sh</code> 管，负责落地验证）；<strong>resume 也不是动词</strong>（各 harness 恢复语义不统一，<code>relaunch</code> 在所有适配器上都能覆盖同一需求，因为磁盘上的 brief 才是持久指令）。<code>relaunch</code> 是唯一改持久记录的动词，因此以事务方式运行：带 journal、安全检查点、ship/scout 强制要求 <code>--note</code> 把接续指令写进新 agent 的任务书。</p><h3 id="子代理委派守卫-fm-subagent-pretool-check-sh" tabindex="-1">子代理委派守卫：fm-subagent-pretool-check.sh <a class="header-anchor" href="#子代理委派守卫-fm-subagent-pretool-check-sh" aria-label="Permalink to &quot;子代理委派守卫：fm-subagent-pretool-check.sh&quot;">​</a></h3><p>这是一次真实事故催生的 PreToolUse 守卫：某 primary 绕过 <code>bin/fm-spawn.sh</code>，用 Claude Code 内置 subagent 工具跑了四个 worker——结果 fleet 视图全程零在途（没写 <code>state/&lt;id&gt;.meta</code> 与 brief）、primary 重启时两个 worker 中途死亡工作丢失、监督循环静默停摆 73 分钟。</p><p>根因：<strong>只有 <code>fm-spawn.sh</code> 会写任务元数据</strong>，harness 自带的委派工具产出的工作对监督栈完全不可见。所以围栏必须拦在 harness 工具面上。守卫按「工具名形状」分类：规范化小写后包含 <code>agent/subagent/task/workflow/cron/schedul/worktree/delegate/spawn/dispatch/handoff/remote/sendmessage/monitor</code> 等词干的委派形工具一律拒绝，同时保留三类豁免——<code>mcp__</code> 前缀（MCP 工具命名不可控）、只观察/停止型（<code>taskoutput/taskstop/killshell</code> 等，拒绝它们会让在途任务无法收尾）、计划型（<code>taskcreate/taskupdate</code>，只写会话内 todo）。</p><h3 id="watcher-武装安全带-fm-arm-pretool-check-sh" tabindex="-1">watcher 武装安全带：fm-arm-pretool-check.sh <a class="header-anchor" href="#watcher-武装安全带-fm-arm-pretool-check-sh" aria-label="Permalink to &quot;watcher 武装安全带：fm-arm-pretool-check.sh&quot;">​</a></h3><p>primary 必须通过可观察的 harness 调用来武装 <code>fm-watch-arm.sh</code>。若命令被包进后台操作符、管道、重定向或无关命令列表，watcher 子进程可能随工具调用一起死掉、失败被隐藏。安全带在执行前拒绝这些命令形状：分类器只做词法分词与执行位置分析，<strong>绝不执行/展开命令的任何部分</strong>；快路径先做字节归一化预判（含 ANSI-C <code>$&#39;...&#39;</code> 与 locale <code>$&quot;...&quot;</code> 编码标记检测），任何 <code>fm-watch</code> 痕迹或编码标记都交给完整分类器裁决。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><ul><li>传统轮询监督既浪费 token 又反应迟钝；FirstMate 用零 token bash watcher 取代之——平时睡眠，仅在可行动事件出现时才唤醒 firstmate。</li><li>可行动事件先写入持久的 <code>state/.wake-queue</code>，被打断也能恢复；良性噪音被吸收并记入 <code>.watch-triage.log</code>。</li><li>六种 primary harness 各有监督适配：Claude Code 用 <code>Stop</code> hook + <code>asyncRewake</code> tokenless re-arm，Grok 用 background-notify，Pi 用 tracked extension，Cursor 停靠 stop hook，Codex 用有界前台检查点，OpenCode 用 TUI plugin。</li><li>turn-end guard 保证「no turn ends blind」：有在途工作而无新鲜 watcher 信标时，阻止盲停或强制有界跟进；信标宽限默认 300 秒（<code>FM_GUARD_GRACE</code>）。</li><li>整个体系 restart-proof：状态全在磁盘与会话后端中，kill 会话后新会话通过 reconcile 接续，确认死亡的 agent 重启、存疑的保持不动。</li><li>干预走控制平面（<code>fm-control.sh</code> 白名单动词 interrupt/exit/relaunch），与数据平面严格分离；subagent-guard 拦截绕过 <code>fm-spawn.sh</code> 的委派形工具，arm-pretool-check 拒绝会隐藏失败的后台/管道/编码混淆命令。</li></ul>`);
      _push(ssrRenderComponent(_component_Quiz, { items: quiz }, null, _parent));
      _push(`<h2 id="🛠️-动手实践" tabindex="-1">🛠️ 动手实践 <a class="header-anchor" href="#🛠️-动手实践" aria-label="Permalink to &quot;🛠️ 动手实践&quot;">​</a></h2><ol><li>在你的 firstmate 克隆中找到 <code>bin/fm-watch.sh</code> 和 <code>bin/fm-turnend-guard.sh</code>，通读两者的头部注释（header comment），用自己的话写出 watcher 的睡眠—分类—唤醒循环与 guard 的判定谓词，对照本章验证理解是否一致。</li><li>检查你所选 harness 的钩子注册文件：若使用 Claude Code，打开 <code>.claude/settings.json</code> 找到两个 <code>Stop</code> hook 条目；若使用 Pi，列出 <code>.pi/extensions/</code> 下的 tracked 扩展文件。确认每个条目对应本章表格中的哪一种接入机制。</li><li>人为制造一次「重启恢复」：在有 crewmate 任务运行时 kill 掉 primary 会话，重新启动后观察 session start 的 reconcile 输出——哪些任务被接续？是否有 confirmed-dead 的 endpoint 被重启？把观察到的时间线记录成一份简报。</li></ol></div>`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("firstmate/ch11.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
export {
  __pageData,
  _sfc_main as default
};
