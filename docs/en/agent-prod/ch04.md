# Chapter 4 · LangGraph StateGraph

> Chapter Goals:
> - Understand LangGraph 定位低层级编排别于 LangChain agents
> - Master StateGraph 最小示例、、、
> - Understand MessagesState 自
> - 区分 LangGraph Runtime Deep s Harness

## 4.1 为 LangGraph

LangChain agents 开箱架构 ReAct、Plan-and-Execute缺乏对流程精细控制LangGraph 填补空白

> LangGraph 低层级编排时于构建、管理长期 它允许你将确定性逻辑 LLM 驱动决策混合同图中

核心差异

| 特性 | LangChain s | LangGraph |
|---|---|---|
| 抽象层级 | 高级 | 低层级 |
| 流程控制 | 黑盒循环 | 显式图结构 |
| | 限 | 内置 Checkpointer |
| Human-in-the-loop | | 原生 |
| 调试 | 困难 | LangSmith 可视 |

## 4.2 最小 Hello World

```python
from langgraph.graph import StateGraph, MessagesState, START, END

# 定义节点：简单的 LLM 调用
def mock_llm(state: MessagesState) -> dict:
    return {
        "messages": [
            {"role": "assistant", "content": "Hello from LangGraph!"}
        ]
    }

# 构建图
graph = StateGraph(MessagesState)
graph.add_node("llm", mock_llm)
graph.add_edge(START, "llm")
graph.add_edge("llm", END)
compiled = graph.compile()

# 执行
result = compiled.invoke({
    "messages": [{"role": "user", "content": "hi!"}]
})
print(result["messages"][-1]["content"])
```

## 4.3 StateGraph core concepts

```python
from langgraph.graph import StateGraph, MessagesState, START, END
from typing import TypedDict

# 自定义状态：在 MessagesState 基础上添加 metadata
class AgentState(MessagesState):
    context: list[str]  # 检索到的上下文
    step_count: int     # 已执行步骤数
    final_answer: str   # 最终答案

# 各节点函数
def search_node(state: AgentState) -> dict:
    """检索相关文档"""
    return {
        "context": ["文档A内容", "文档B内容"],
        "step_count": state.get("step_count", 0) + 1
    }

def llm_node(state: AgentState) -> dict:
    """调用 LLM 生成回答"""
    context = " ".join(state.get("context", []))
    return {
        "messages": [{"role": "assistant", "content": f"基于上下文: {context}"}],
        "step_count": state["step_count"] + 1
    }

# 构建图
workflow = StateGraph(AgentState)
workflow.add_node("search", search_node)
workflow.add_node("llm", llm_node)
workflow.add_edge(START, "search")
workflow.add_edge("search", "llm")
workflow.add_edge("llm", END)

graph = workflow.compile()
```

## 4.4 LangChain s

```python
# LangGraph 可以与 LangChain 组件结合使用
from langchain.chat_models import ChatOpenAI
from langchain.tools import tool

@tool
def get_weather(city: str) -> str:
    """获取城市天气"""
    return f"{city} 今天晴朗，25°C"

# 使用 LangChain 的 LLM + LangGraph 的图编排
llm = ChatOpenAI(model="gpt-4o-mini")
# ... 将 LLM 调用封装为图节点
```

> 💡 更见 [第 5 章](./ch05)

## 4.5 模式

```python
# 同步执行
result = graph.invoke({"messages": [...]})

# 流式执行（见第 8 章）
for event in graph.stream({"messages": [...]}):
    print(event)

# 异步执行
async_result = await graph.ainvoke({"messages": [...]})
```

## 本章Summary

- LangGraph 低层级编排时精细图结构控制
- `StateGraph` + `MessagesState` 最小起步单元
- 自继承 `MessagesState` 并扩展字段
- LangChain 组件可混合

## 🛠️ Hands-on Practice

1. Implement最小 LangGraph 应两个 greeted → end输出 "Hello LangGraph!"
2. 扩展 StateGraph添加自字段 `conversation_id`
3. `graph.stream()` Implement


<Quiz :items="quiz" />
<script setup>
const quiz = [
{
q: 'LangGraph 核心定位？',
options: ['高级 ', '低层级编排时', 'LLM 供应商', '工具'],
answer: 1,
explain: 'LangGraph 低层级控制让开发者精确编排 流程'
},
{
q: 'StateGraph 第参数？',
options: ['列表', '类型TypedDict MessagesState', '列表', '器'],
answer: 1,
explain: 'StateGraph 泛型参数结构MessagesState 最常内置类型'
},
{
q: 'START END 作？',
options: ['图入口出口', ' LLM 调', '工具', '错误'],
answer: 0,
explain: 'START 图起点END 终止点通 add_edge 连接'
},
{
q: 'LangGraph LangChain s 区别？',
options: ['LangGraph 更高级', 'LangGraph 更精细流程控制', '没区别', 'LangChain 更多模型'],
answer: 1,
explain: 'LangGraph LangChain 上层时显式图结构'
}
];
</script>
