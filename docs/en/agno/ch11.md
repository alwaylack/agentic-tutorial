# Chapter 11 · Workflow ：Steps 

> Chapter goals: Workflow  Team ， `Workflow` + `Step`  Agent、Team 。

## 11.1  Workflow

Team ""：leader 、。，——。****：

- ：、、，；
- ： →  → ，；
- ：，。

Agno ：

| | Team | Workflow |
|---|---|---|
|  | Leader  |  |
|  | 、 | 、 |
|  | mode /  /  | Step / Parallel / Condition / Loop / Router |

## 11.2 ： → 

`Workflow` ，`steps` 。 `Step` ：Agent、Team、 Python ， Workflow。

```python
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.workflow import Step, StepInput, StepOutput, Workflow

model = OpenAIChat(
    id="deepseek-chat",
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

writer = Agent(
    name="Writer",
    model=model,
    instructions=["根据给定主题写一篇 300 字以内的短文。"],
)

reviewer = Agent(
    name="Reviewer",
    model=model,
    instructions=[
        "审校上一步的文章：检查事实表述、语法和结构。",
        "直接输出修改后的最终版本。",
    ],
)

# 撰写 -> 审校，顺序由代码保证
workflow = Workflow(
    name="writing-pipeline",
    steps=[
        Step(name="draft", executor=writer),
        Step(name="review", executor=reviewer),
    ],
)

workflow.print_response(
    "主题：为什么自动化测试值得投入", markdown=True
)
```

 `draft`  `review` ——。

## 11.3 Step 

```python
from agno.team import Team
from agno.workflow import Step, StepInput, StepOutput, Workflow


def data_preprocessor(step_input: StepInput) -> StepOutput:
    """函数执行器：做确定性加工，不消耗 token"""
    research = step_input.previous_step_content or "没有收到调研内容"
    return StepOutput(content=f"待分析的调研材料:\n{research}")


research_team = Team(name="Research Team", members=[...], model=model)

workflow = Workflow(
    name="mixed-pipeline",
    steps=[
        research_team,        # 形态一：直接放 Agent/Team（自动包装成 Step）
        data_preprocessor,    # 形态二：直接放函数
        Step(                 # 形态三：显式 Step 包装，推荐用于需要命名/描述的场景
            name="analysis",
            executor=writer,
            description="基于调研产出分析报告",
        ),
    ],
)
```

。（`name=`）：。

## 11.4 StepInput  StepOutput：

—— `StepInput`， `StepOutput`：

```python
def summarize(step_input: StepInput) -> StepOutput:
    # input：本次工作流的原始输入（始终保留）
    original = str(step_input.input)
    # previous_step_content：上一个步骤输出的正文
    prev = step_input.previous_step_content or ""
    # previous_step_outputs：若上游是并行块，可拿到各命名步骤的输出字典
    return StepOutput(
        content=f"原始需求: {original}\n\n材料摘要:\n{prev[:500]}",
    )
```

：

| StepInput  |  |
|---|---|
| `input` |  |
| `previous_step_content` |  |
| `previous_step_outputs` | （） |
| `additional_data` |  |

::: tip 
 Agent  Workflow ：、、 token， Python 。
:::

## 11.5 

`print_response()` ； `run()` ：

```python
result = workflow.run("主题：向量数据库选型")

# workflow_output.content 是最后一个步骤的输出
print(result.content)

# 更细粒度：逐步查看每个步骤的执行结果
for step_result in (result.step_results or []):
    print(step_result.step_name, "->", str(step_result.content)[:80])
```

 `arun()` ， `run(..., stream=True)` 。

## 11.6 

- Team = ；Workflow = ；
- `Step`  Agent、Team、 Workflow， steps ；
-  `StepInput.previous_step_content` ，`StepOutput(content=...)` ；
-  `run()`  `step_results` ，。

<Quiz :items="quiz" />

## 🛠️ 

<script setup>
const quiz = [
  {
    question: '关于 Agno 中 Team 和 Workflow 的分工，正确的是？',
    options: [
      'Workflow 比 Team 更智能，应完全替代 Team',
      'Team 由 leader 模型自主委派，Workflow 由开发者代码定义控制流',
      '两者不能包含相同的 Agent',
      'Workflow 只能编排函数，不能编排 Agent'
    ],
    answer: 1,
    explain: '官方定位：Team 适合需要模型自主分派的协作场景，Workflow 适合需要确定性执行路径的流程；Workflow 的 Step 可以包裹 Agent、Team 或函数。'
  },
  {
    question: 'Workflow 中一个 Step 的合法执行器不包括以下哪种？',
    options: ['一个 Agent', '一个 Team', '一个接受 StepInput 返回 StepOutput 的函数', '任意 Pydantic BaseModel 实例'],
    answer: 3,
    explain: 'Step 包裹的执行器是 Agent / Team / 自定义函数（含类实例 __call__）/ 嵌套 Workflow；Pydantic 模型用于 output_schema 结构化输出，不是执行器。'
  },
  {
    question: '函数步骤中想获取「上一个步骤的输出」，应该读取？',
    options: ['step_input.input', 'step_input.previous_step_content', 'step_output.previous_content', 'workflow.last_output'],
    answer: 1,
    explain: 'previous_step_content 存放上一步骤输出的正文；step_input.input 始终是工作流的原始用户输入，二者含义不同。'
  },
  {
    question: '为什么推荐在两个 LLM 步骤之间插入纯 Python 函数步骤？',
    options: [
      '函数步骤可以提升模型的推理能力',
      '格式转换、校验等确定性工作不需要消耗 token，交给代码更快更稳',
      'Agno 要求每两个 Agent 之间必须有函数',
      '函数步骤可以绕过 API 限流'
    ],
    answer: 1,
    explain: '确定性加工（格式化、截断、校验）交给 Python 既省钱又稳定，这是官方文档中 custom function step 的核心用途之一。'
  }
]
</script>

1.  9 ，"→→"。
2.  `word_limit`， 400 。
3.  `run()` ， `step_result`  100 。

>  Workflow ：[、](./ch12)。
