# Chapter 17 ·  Knowledge  RAG 

> Chapter goals: RAG ， Agno  `Knowledge` 、 LanceDB  Embedding ， Agent 。

## 17.1 RAG 

，、，（）。**RAG（Retrieval-Augmented Generation，）**：""，""。

：

1. **（Indexing）**： → （chunking）→ （embedding）→ ；
2. **（Retrieval）**： →  →  → 。

::: tip 
- **Embedder**：（），；
- **Vector DB**：""，Agno  LanceDB、PgVector、Chroma ；
- **Knowledge**：Agno ，、、、。
:::

## 17.2  Knowledge 

`Knowledge`  Agno （3.x  API）。 Embedder， `insert()` ——、URL、， PDF、DOCX、CSV、Markdown  Reader：

```python
# knowledge_basic.py
import os
from agno.knowledge.knowledge import Knowledge
from agno.knowledge.embedder.openai import OpenAIEmbedder
from agno.vectordb.lancedb import LanceDb, SearchType

# 本地向量库 LanceDB：无需单独部署服务，数据落在本地目录
knowledge = Knowledge(
    vector_db=LanceDb(
        table_name="company_docs",          # 向量表名
        uri="tmp/lancedb",                  # 数据存储目录
        search_type=SearchType.vector,      # 向量检索（后续章节讲混合检索）
        embedder=OpenAIEmbedder(            # 三方 OpenAI 兼容 Embedding 服务
            id="Qwen/Qwen3-Embedding-8B",   # 以你的平台实际提供的模型 id 为准
            api_key=os.getenv("EMBEDDING_API_KEY"),
            base_url="https://api.siliconflow.cn/v1",
        ),
    )
)

# 灌入内容：支持 url / path / 目录 / 纯文本
knowledge.insert(
    url="https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf",
    skip_if_exists=True,     # 已入库的内容跳过，避免重复索引
)
knowledge.insert(text_content="公司年假政策：入职满一年享有 5 天带薪年假……")
```

：

```bash
pip install "agno[lancedb]" pypdf   # lancedb 与 PDF 读取支持
```

::: warning 
`insert()`  Embedding API ； `skip_if_exists=True`，。
:::

## 17.3  Agent 

 `Agent`  `knowledge=` 。 Agno  `search_knowledge_base` ，****—— Agentic RAG：

```python
# rag_agent.py
from agno.agent import Agent
from agno.models.openai import OpenAIChat

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    knowledge=knowledge,
    search_knowledge=True,   # 提供知识库时默认开启，显式写出更清晰
    instructions=[
        "回答前先搜索知识库。",
        "回答末尾注明引用了哪些来源。",
        "知识库里没有的内容，明确说不知道，不要编造。",
    ],
    markdown=True,
)

agent.print_response("泰式椰奶鸡汤怎么做？")
```

，（），。

## 17.4 

—— `knowledge.search()` ，：

```python
results = knowledge.search(query="年假有多少天")
for r in results:
    print("匹配度:", getattr(r, "score", None))
    print("内容:", r.content[:100])
    print("元数据:", r.meta_data)       # 含来源文件名等，可用于溯源
```

（ RAG ）， `add_knowledge_to_context=True`；。

```python
# 传统 RAG：每次提问前固定执行一次检索并拼进上下文
traditional_rag_agent = Agent(
    model=model,
    knowledge=knowledge,
    add_knowledge_to_context=True,   # 检索结果自动加入用户消息
)
traditional_rag_agent.print_response("公司年假政策是什么？")
```

## 17.5 

- **Embedding ""**： Embedder ，；
- ****： Embedding ，（ Qwen/BGE ）；
- ****：`uri` ；
- ****： chunk ， 18 。

## 

- RAG = （→→）+ （→→）；
- `Knowledge(vector_db=LanceDb(...), embedder=OpenAIEmbedder(...))` ，LanceDB ；
- `Agent(knowledge=..., search_knowledge=True)` ；
- `knowledge.search()` ；
-  Embedding 。

<Quiz :items="quiz" />

## 🛠️ 

1.  LanceDB +  Embedding ， 3 ， `knowledge.search()`  5 。
2.  17.3  RAG Agent ，， instructions ""。
3.  insert  PDF URL，。

>  RAG ，[ 18 ：RAG ](./ch18)、。

<script setup>
const quiz = [
  {
    question: '在 RAG 的写入阶段，文档经历的正确处理顺序是？',
    options: ['向量化 → 切块 → 入库', '切块 → 向量化 → 入库', '入库 → 切块 → 向量化', '切块 → 入库 → 向量化'],
    answer: 1,
    explain: '正确顺序是先切块（chunking）把长文档拆成小片段，再对每个片段做 embedding 向量化，最后连同向量一起写入向量数据库；顺序颠倒无法建立正确的索引。'
  },
  {
    question: '给 Agent 传入 knowledge 参数后，Agno 默认的行为是？',
    options: [
      '每次提问前强制检索全部内容塞进提示词',
      '自动注册 search_knowledge_base 工具，由模型自主决定何时检索',
      '把所有文档全文放进 system prompt',
      '拒绝回答知识库之外的问题'
    ],
    answer: 1,
    explain: 'search_knowledge=True 时 Agno 会注册检索工具，是否调用、用什么关键词调用由模型自主决定，这正是 Agentic RAG 的特征；传统 RAG 才是"先检索后回答"。'
  },
  {
    question: '关于 Embedding 配置，下列说法错误的是？',
    options: [
      '入库和检索应使用同一个 Embedding 模型',
      '更换 Embedding 模型后需要重建向量表',
      '只要模型名相似就可以混用不同的 Embedding 模型',
      '中文场景应选择支持中文的多语种 Embedding 模型'
    ],
    answer: 2,
    explain: '不同 Embedding 模型的向量空间完全不同，混用会导致相似度计算毫无意义；换模型必须重新对全部内容做向量化并重建索引。'
  },
  {
    question: '想在不调用大模型的情况下验证"某个问题能不能检索到正确片段"，应该怎么做？',
    options: ['直接问 Agent 并看它怎么答', '调用 knowledge.search() 检查返回结果', '把 temperature 调成 0', '提高 max_tokens'],
    answer: 1,
    explain: 'knowledge.search() 只走检索链路不经过 LLM，能独立验证召回质量；temperature 和 max_tokens 都是生成阶段的参数，与检索无关。'
  }
]
</script>
