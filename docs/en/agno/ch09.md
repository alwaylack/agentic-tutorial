# Chapter 9 · Agent Team ：

> Chapter goals: Agno Team "-"， coordinate / route / broadcast / tasks ， researcher + writer 。

## 9.1  Team

 Agent ：，，。**Team**  Agent（members），**（leader）**、——：，。

::: warning  Team
Team ，、token 。： Agent，""。
:::

```python
from agno.agent import Agent
from agno.team import Team

team = Team(
    members=[
        Agent(name="英语客服", role="用英语回答客户问题"),
        Agent(name="中文客服", role="用中文回答客户问题"),
    ],
)
```

## 9.2 

Agno  `mode` （ `agno.team.mode`  `TeamMode`），：

|  |  |  |
|---|---|---|
| **Coordinate**（） | `mode=TeamMode.coordinate` | 、、 |
| **Route** | `mode=TeamMode.route` | ****， |
| **Broadcast** | `mode=TeamMode.broadcast` | ****， |
| **Tasks** | `mode=TeamMode.tasks` | ， |

::: info 
 `respond_directly=True`（ route） `delegate_to_all_members=True`（ broadcast）， `mode=`； mode 。
:::

```python
from agno.team import Team, TeamMode

router_team = Team(
    name="语言路由台",
    members=[english_agent, chinese_agent],
    mode=TeamMode.route,
    determine_input_for_members=False,   # 用户原话直传成员，不经领导者改写
)
```

：** coordinate， route， broadcast， tasks**。

```python
from agno.team import Team, TeamMode

# broadcast：同一任务发给全部成员再汇总（旧写法 delegate_to_all_members=True）
review_team = Team(
    name="多视角评审团",
    model=model,
    members=[lawyer, engineer, marketer],
    mode=TeamMode.broadcast,
)

# tasks：领导者维护共享任务清单逐项推进，适合长链路交付
delivery_team = Team(
    name="交付小组",
    model=model,
    members=[planner, developer, tester],
    mode=TeamMode.tasks,
)
```

## 9.3 

Coordinate ：

1. Team ；
2. ，；
3. ****（）；
4. （）；
5. 。

（）。 `id` 。

## 9.4 ： + 

```python
# research_writer_team.py
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.team import Team, TeamMode
from agno.tools.duckduckgo import DuckDuckGoTools

model = OpenAIChat(
    id="deepseek-chat",
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

researcher = Agent(
    name="研究员",
    id="researcher",
    role="负责联网搜集资料，只输出事实与来源",
    tools=[DuckDuckGoTools()],           # 研究员专属工具：搜索
)

writer = Agent(
    name="作家",
    id="writer",
    role="负责把资料改写成流畅的中文短文，不做事实核查",
)

team = Team(
    name="内容编辑部",
    model=model,                          # 领导者也需一个模型做分派与综合
    members=[researcher, writer],
    mode=TeamMode.coordinate,
    instructions=[
        "先委派研究员收集素材，再把素材交给作家成文。",
        "最终输出一篇 300 字以内的短文，并附资料来源。",
    ],
    markdown=True,
)

team.print_response("介绍一下 MCP 协议的核心价值")
```

：，，。

## 9.5 

- ****： `debug_mode=True`  metrics，；
- ****：`instructions` "、"，；
- ****： 2–4 ； Team  Workflow ；
- ****：，。

## 

- Team = （/）+ （），；
- `TeamMode` ：coordinate（）、route（）、broadcast（）、tasks（）， `mode=` ；
- Coordinate ： →  →  →  → ；
- `determine_input_for_members=False` ；
-  Agent  Team， token 。

<Quiz :items="quiz" />

## 🛠️ 

1.  9.4  `route` ""，。
2.  `broadcast` ，。
3.  `@tool` ，。

> ， Team ：[ 10 ：Team ](./ch10)；[ 11 ](./ch11) Workflow 。

<script setup>
const quiz = [
  {
    question: 'Agno 中 Team 默认的协作模式是？',
    options: ['route', 'broadcast', 'coordinate', 'tasks'],
    answer: 2,
    explain: '不设置 mode 时即为 coordinate：领导者挑选成员、为其生成子任务并把各成员结果综合成最终回复。'
  },
  {
    question: '希望"同一个问题发给所有成员、再汇总多方观点"，应该用哪种模式？',
    options: ['coordinate', 'route', 'broadcast', 'tasks'],
    answer: 2,
    explain: 'broadcast 模式会把同一任务广播给全部成员（旧写法 delegate_to_all_members=True），适合多角度交叉验证类场景。'
  },
  {
    question: '关于 Team 与单 Agent 的取舍，下列说法正确的是？',
    options: [
      'Team 总是比单 Agent 效果好，应尽量多用',
      'Team 的每次分派都是独立模型调用，会增加延迟与 token 成本，单 Agent 能胜任时不要上 Team',
      'Team 不支持给成员单独配置工具',
      'route 模式会让所有成员同时执行'
    ],
    answer: 1,
    explain: '领导者与成员是分开的模型调用，协调本身有开销；官方文档明确建议单一领域能完成的任务使用单 Agent。Team 支持成员级工具配置，route 只路由给一个成员。'
  },
  {
    question: '在 route 模式下想让用户的原始输入不加改写地传给成员，应设置什么参数？',
    options: ['passthrough=True', 'determine_input_for_members=False', 'raw_input=True', 'forward_original=True'],
    answer: 1,
    explain: '默认情况下领导者会为成员加工任务描述；设置 determine_input_for_members=False 后用户输入将原样传递给被路由到的成员。'
  }
]
</script>
