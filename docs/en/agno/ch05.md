# Chapter 5 · output output_schema

> Chapter goals： Agent output""" Pydantic "，Master output_schema 、。

## 5.1 

" Agent"，。model， `json.loads` ——model、，。**output**：，model，。

## 5.2 ：output_schema + Pydantic

```python
# structured_basic.py ——  Agent  Pydantic 
import os
from dotenv import load_dotenv
from pydantic import BaseModel, Field
from agno.agent import Agent
from agno.models.openai import OpenAIChat

load_dotenv()

class MovieScript(BaseModel):
    setting: str = Field(description="")
    genre: str = Field(description="")
    storyline: str = Field(description="")

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    output_schema=MovieScript,     # ：output
)

response = agent.run("")

print(type(response.content))          # <class 'MovieScript'>，！
print(response.content.setting)        # 
print(response.content.genre)
print(response.content.storyline)
```

`response.content`  `MovieScript` ——tip、IDE 、。

## 5.3 ：schema 

UnderstandTroubleshoot。 `output_schema`  Agno ：

1.  Pydantic model **JSON Schema**；
2. modeloutput（ OpenAI  JSON Schema ），；
3. ， schema tipmodeloutput JSON，；
4.  Pydantic ****，Error—— `content` 。

::: warning 
DeepSeek model"tip + "，。documentation：** schema  `content` **。

```python
# safe_access.py —— 
result = agent.run("")
if isinstance(result.content, MovieScript):
    print(":", result.content.genre)
else:
    print("，:", result.content)   # 
```
:::

## 5.4 ：model、

output。Pydantic ：

```python
# structured_nested.py —— run
from pydantic import BaseModel, Field
from agno.agent import Agent
from agno.models.openai import OpenAIChat
import os
from dotenv import load_dotenv

load_dotenv()
model = OpenAIChat(
    id="deepseek-chat",
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

class Ingredient(BaseModel):
    name: str = Field(description="")
    amount: str = Field(description="， 200g")

class Recipe(BaseModel):
    title: str
    difficulty: str = Field(description="// ")
    ingredients: list[Ingredient] = Field(description="")   # 
    steps: list[str] = Field(description="")

agent = Agent(model=model)   #  schema

#  Agent  schema ——  Agent output
recipe = agent.run(" 20 ", output_schema=Recipe).content
print(recipe.title, "", len(recipe.ingredients), "")
for ing in recipe.ingredients:
    print(f"- {ing.name}: {ing.amount}")
```

（`run(..., output_schema=...)`）" Agent "，output Agent。

## 5.5  + tool

，output：

```python
# old_way.py —— ： JSON 
import json

resp = agent.run(" JSON ， title、ingredients、steps")
try:
    # model JSON "，..."，
    data = json.loads(resp.content)
except json.JSONDecodeError as e:
    print("，：", e)
    #  strip 、 {...}、...
```

outputtool：Agent tool， schema 。example——tool，`StockAnalysis` model。"**、**" Agent 。

## 5.6 Schema 

，：

1. ** `Field(description=...)`**——modelnote，；
2. ****：`Field(description=" buy/hold/sell ")`  `Literal["buy", "hold", "sell"]`；
3. ** `ge/le`**： `confidence: float = Field(ge=0, le=1)`，；
4. ****（ ≤3 ）：，modelError。

## 5.7 Summary

- `output_schema`  `content`  Pydantic ；
- "JSON Schema  →  → Pydantic "，modeltip；
-  `isinstance` ；
- schema 、run， Agent output；
- ，output。

<Quiz :items="quiz" />

## 🛠️ Practice

<script setup>
const quiz = [
  {
    question: ' output_schema ，response.content ？',
    options: ['JSON ', ' Pydantic model', 'dict ', 'bytes'],
    answer: 1,
    explain: 'Agno modeloutput Pydantic ，tip。'
  },
  {
    question: 'outputmodel，Agno ？',
    options: [
      'run',
      ' GPT-4',
      ' schema tipoutput JSON， Pydantic ',
      ''
    ],
    answer: 2,
    explain: 'documentationnote； content ，。'
  },
  {
    question: ' Agent ，？',
    options: [
      ' Agent',
      ' run()  output_schema ',
      'model',
      ' schema'
    ],
    answer: 1,
    explain: 'output_schema run， Agent output，。'
  },
  {
    question: ' Pydantic  sentiment ？',
    options: [
      'sentiment: str',
      'sentiment: str = Field(description="")',
      'sentiment: Literal["positive", "negative", "neutral"]',
      'sentiment: list'
    ],
    answer: 2,
    explain: 'Literal  JSON Schema ，model； str 。'
  }
]
</script>

1. "" Pydantic model（：category、urgency 1-5、summary）， Agent  5 output。
2.  5.3 ""，tip"output JSON"。
3. model（""""，""），modeloutput。

> output，[ 6 ：Prompt ——instructions ](./ch06)。
