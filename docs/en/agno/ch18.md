# Chapter 18 · RAG ：Agentic RAG 

> Chapter goals: Agentic RAG  RAG ，、。

## 18.1 Agentic RAG vs  RAG

 **Agentic RAG**： `search_knowledge_base` ，"、"，。** RAG**——，：

```python
from agno.agent import Agent

# 传统 RAG：每次请求前先检索，参考内容自动拼进用户消息
traditional = Agent(
    model=model,
    knowledge=knowledge,
    add_knowledge_to_context=True,   # 检索结果直接加入上下文
)

# Agentic RAG：模型自主决定是否/如何检索（默认行为）
agentic = Agent(
    model=model,
    knowledge=knowledge,
    search_knowledge=True,           # 注册检索工具，交给模型决策
)
```

|  |  RAG | Agentic RAG |
|---|---|---|
|  |  |  |
|  |  |  |
|  |  | 、 |
|  | 、 |  |

::: tip 
 FAQ "" RAG ； Agentic RAG。。
:::

## 18.2 ：

 chunk。，；，。Agno  `Chunking` ， Reader ：

```python
from agno.knowledge.chunking.fixed import FixedSizeChunking
from agno.knowledge.chunking.recursive import RecursiveChunking
from agno.knowledge.chunking.document import DocumentChunking
from agno.knowledge.reader.pdf_reader import PDFReader
from agno.knowledge.knowledge import Knowledge

# 固定长度：简单粗暴，适合格式规整的文本
fixed_reader = PDFReader(chunking_strategy=FixedSizeChunking(size=1500))

# 递归切块：按段落→句子→字符多级分隔符逐级回退，通用性最好
recursive_reader = PDFReader(chunking_strategy=RecursiveChunking())

# 文档结构切块：按标题/段落等文档结构边界切，保持章节完整
doc_reader = PDFReader(chunking_strategy=DocumentChunking())

knowledge = Knowledge(vector_db=vector_db)
knowledge.insert(path="handbook.pdf", reader=recursive_reader)
```

：Markdown/HTML 、 Code Chunking、， Semantic Chunking。

## 18.3 ：""

，，"""//"：

```python
# 入库时附加 metadata
knowledge.insert(
    path="resumes/",
    metadata={"department": "engineering", "year": 2025},
)

# 方式一：Agent 级固定过滤——这个 Agent 永远只看 engineering 的文档
agent = Agent(
    model=model,
    knowledge=knowledge,
    search_knowledge=True,
    knowledge_filters={"department": "engineering"},
)

# 方式二：调用时临时过滤，多个条件之间是 AND 关系
agent.print_response(
    "总结 2025 年入职的工程师简历亮点",
    knowledge_filters={"year": 2025},
)

# 直接检索同样支持 filters
results = knowledge.search(query="Python 经验", filters={"department": "engineering"})
```

 `enable_agentic_knowledge_filters=True` （"Jordan " `user_id=jordan`）， Contents DB 。

## 18.4  Reranker：

""。**Reranker** ，：

```python
import os
from agno.knowledge.reranker.cohere import CohereReranker
from agno.vectordb.lancedb import LanceDb, SearchType

knowledge = Knowledge(
    vector_db=LanceDb(
        uri="tmp/lancedb",
        table_name="docs",
        search_type=SearchType.hybrid,   # 混合检索：向量 + 关键词双信号
        embedder=embedder,
        reranker=CohereReranker(model="rerank-v3.5"),   # 精排模型
    ),
)
```

：****；****。 RAG 。

## 18.5 

，：

1.  20–50 「 → 」；
2.  `knowledge.search()` ，（top-k ）；
3. / Embedding /。

```python
def hit_rate(kb, cases, k=5):
    hits = sum(
        any(c["keyword"] in r.content for r in kb.search(q=c["q"], top_k=k))
        for c in cases
    )
    return hits / len(cases)   # 例如 0.85 表示 85% 的问题召回了目标片段
```

## 

- Agentic RAG（`search_knowledge=True`，） RAG（`add_knowledge_to_context=True`，）；
-  Reader  `chunking_strategy` ，；
- `knowledge_filters` ，`enable_agentic_knowledge_filters=True` ；
- `SearchType.hybrid` + Reranker ；
- 。

<Quiz :items="quiz" />

## 🛠️ 

1.  PDF  `FixedSizeChunking(size=1000)`  `RecursiveChunking()` ， `search()` 。
2.  `{"lang": "zh"/"en"}` ，。
3.  10  `hit_rate` ，。

> [ 19 ： Reasoning](./ch19)，""。

<script setup>
const quiz = [
  {
    question: 'Agentic RAG 与传统 RAG 的核心区别是什么？',
    options: [
      'Agentic RAG 不需要向量数据库',
      'Agentic RAG 由模型自主决定何时以及如何检索',
      '传统 RAG 不使用 Embedding',
      '传统 RAG 只能处理 PDF'
    ],
    answer: 1,
    explain: 'Agentic RAG 把检索作为工具交给模型按需调用（可改写查询、多次检索）；传统 RAG 则是每次请求都固定执行一次检索并把结果注入上下文。两者都需要 Embedding 和向量库。'
  },
  {
    question: '关于切块（chunking），下列说法正确的是？',
    options: [
      '块越大越好，包含的信息越多',
      '块越小越好，检索越精确',
      '块太大稀释相似度、块太小语义不完整，需按内容特点权衡',
      '切块策略只在查询阶段起作用'
    ],
    answer: 2,
    explain: '切块发生在写入阶段。过大的块混入无关内容会稀释向量相似度，过小的块丢失上下文导致语义不完整；应依据文档结构选择策略并实验验证。'
  },
  {
    question: '设置 knowledge_filters={"year": 2025} 后又传入 {"dept": "sales"}，两个过滤条件如何生效？',
    options: ['OR 关系，命中任一即可', 'AND 关系，必须同时满足', '后者覆盖前者', '报错，只能有一个过滤键'],
    answer: 1,
    explain: '官方文档明确：多个过滤器之间以 AND 逻辑组合，即同时满足 year=2025 且 dept=sales 的内容才会被检索。'
  },
  {
    question: 'Reranker 在检索链路中的作用是？',
    options: [
      '替代 Embedding 模型生成向量',
      '把用户问题翻译成英文',
      '用专门的排序模型对候选结果重新打分排序，提升最终相关性',
      '压缩知识库存储体积'
    ],
    answer: 2,
    explain: 'Reranker（如 Cohere rerank-v3.5）在向量/混合检索给出候选之后做精细重排，属于检索质量的"最后一道关"，不参与向量化也不影响存储。'
  }
]
</script>
