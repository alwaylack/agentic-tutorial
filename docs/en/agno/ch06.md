# Chapter 6 · Prompt ：instructions 

> Chapter goals：Master Agno  `description`、`instructions`、`system_message` ，、 Agent tip。

## 6.1 Agno tip

Agno  system prompt，configuration。documentation（Context Engineering）model：

| configuration |  |  |
|---|---|---|
| `description` |  | Agent 、，"note" |
| `instructions` |  | ，"" |
| `system_message` |  |  system ，**** |

```python
# prompt_layers.py —— 
import os
from dotenv import load_dotenv
from agno.agent import Agent
from agno.models.openai import OpenAIChat

load_dotenv()
model = OpenAIChat(
    id="deepseek-chat",
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

agent = Agent(
    model=model,
    # note：
    description=" Python 。",
    # ：，Agno  system 
    instructions=[
        "，",
        "",
        "，example",
        "，",
    ],
    markdown=True,
)

agent.print_response("")
```

**？** ，model""； diff——tip"configuration"""。

## 6.2 instructions 

instructions ，run，Implement"tip"：

```python
# prompt_template.py —— 
agent = Agent(
    model=model,
    db=None,
    instructions=[
        " {plan}，",
        "note",
    ],
    session_state={"plan": "pro"},     #  {plan}
)

#  Agent  session_state 
agent.print_response("？", session_state={"plan": "free"})
```

""tip： `session_state`，tip。

## 6.3  system_message

 `description + instructions` 。** system ** `system_message`——note：

```python
# system_message_demo.py ——  system 
agent = Agent(
    model=model,
    description="",           # ⚠️  system_message 
    instructions=[""],
    system_message=" pirate Translator，input。",
)
```

::: warning 
 prompt ，** `system_message`**。 Agno 。
:::

## 6.4 Few-shot example

output，（few-shot）。 5 output， schema ；****example：

```python
# few_shot.py —— example
from pydantic import BaseModel, Field
from typing import Literal

class TicketCategory(BaseModel):
    category: Literal["bug", "feature", "question", "complaint"]
    urgency: int = Field(ge=1, le=5, description="")

support_router = Agent(
    model=model,
    description="。",
    instructions=[
        "output(1-5)",
        "example：",
        "- '' → bug, urgency=3",
        '- "" → feature, urgency=2',
        '- "？" → question, urgency=1',
        '- "！" → complaint, urgency=5',
    ],
    output_schema=TicketCategory,
)

cases = ["App ", ""]
for c in cases:
    r = support_router.run(c)
    print(c, "->", r.content.category, r.content.urgency)
```

example****："" bug  complaint？，model。

## 6.5  Prompt 

model，：

1. ****： DeepSeek ，，Understand；
2. ****：""" 25 "；
3. ** 10 **：， Agent；
4. ****：，；
5. ** `additional_guidance` **（）， instructions ，。

## 6.6 ：tip

tip""，Verify：

```python
# prompt_eval.py —— tip
EVAL_CASES = [
    ("", "bug"),
    (" Excel", "feature"),
    ("", "question"),
]

def evaluate(agent) -> float:
    hits = sum(
        agent.run(q).content.category == expect
        for q, expect in EVAL_CASES
    )
    return hits / len(EVAL_CASES)

print(f": {evaluate(support_router):.0%}")
#  instructions ，
```

、 CI ， 25 （Evals）。

## 6.7 Summary

- `description` 、`instructions` 、`system_message` （）；
- instructions  +  `{placeholder}`，"configuration"；
- Few-shot example， output_schema ；
- tip：、、 ≤10 、；
- tipVerify，。

<Quiz :items="quiz" />

## 🛠️ Practice

<script setup>
const quiz = [
  {
    question: ' system_message ，description  instructions ？',
    options: [
      '',
      '，system  system_message ',
      '',
      'streaming'
    ],
    answer: 1,
    explain: 'system_message configuration， Agno  description/instructions  system ，。'
  },
  {
    question: 'instructions  " {plan}"，？',
    options: [
      '，',
      ' run(..., session_state={"plan": ...}) ',
      ' format_plan() ',
      ''
    ],
    answer: 1,
    explain: 'instructions  {} ，run session_state ，Implementtip。'
  },
  {
    question: ' few-shot example，？',
    options: [
      ' token ',
      'output，',
      ' output_schema ',
      'model'
    ],
    answer: 1,
    explain: 'example：model； Pydantic。'
  },
  {
    question: '，tip？',
    options: [
      '',
      '，，',
      'modeltip',
      ''
    ],
    answer: 1,
    explain: 'tip""，：tip。'
  }
]
</script>

1.  5 "Practice" Agent  description + instructions ， 3 。
2.  Agent  10 ，Implement `prompt_eval.py` ：output/。
3. ："output JSON" instructions ， 5 Error。

> ，[ 7 ：tool Tools——tool](./ch07)。
