# Chapter 23 · AgentOS： Agent 

> Chapter goals: AgentOS  Agent/Team/Workflow  FastAPI ， REST 、。

## 23.1 AgentOS 

""。AgentOS  Agno （runtime）：** FastAPI **， API、、、。

```python
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat
from agno.os import AgentOS

db = SqliteDb(db_file="tmp/agentos.db")

support_agent = Agent(
    id="support-agent",            # id 将成为 URL 的一部分，务必显式设置
    name="Support Agent",
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    db=db,
)

agent_os = AgentOS(
    id="product-agent-os",
    agents=[support_agent],
    teams=[],          # 也可以注册 teams=[...], workflows=[...]
    workflows=[],
    db=db,             # 组件未单独配库时继承该默认库
)

app = agent_os.get_app()   # 拿到原生 FastAPI 实例

if __name__ == "__main__":
    agent_os.serve(app="agent_os:app", reload=True)   # 默认监听 7777 端口
```

 `http://localhost:7777` 。`get_app()`  FastAPI ， Uvicorn/Gunicorn/Docker 。

## 23.2  REST 

AgentOS 「 +  id」：

|  |  |
|---|---|
| Agent | `POST /agents/{agent_id}/runs` |
| Team | `POST /teams/{team_id}/runs` |
| Workflow | `POST /workflows/{workflow_id}/runs` |

 curl  Agent：

```bash
curl http://localhost:7777/agents/support-agent/runs \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "message=我的订单到哪了？" \
  -d "user_id=customer-42" \
  -d "session_id=order-support-42" \
  -d "stream=false"
```

：

-  `run_id`  `session_id`；** `session_id` **；
- `stream=true`  SSE ，curl  `-N` ；
-  message， `dependencies`、`session_state`、`metadata`、`knowledge_filters`  `output_schema`（JSON Schema ）。

## 23.3  Python 

 curl。 `httpx` ：

```python
import httpx


class AgentOSClient:
    def __init__(self, base_url: str, agent_id: str, api_key: str | None = None):
        self.base_url = base_url.rstrip("/")
        self.agent_id = agent_id
        headers = {"x-api-key": api_key} if api_key else {}
        self.http = httpx.Client(base_url=self.base_url, headers=headers, timeout=120)

    def info(self) -> dict:
        """标准握手：先探测 auth_mode 与组件数量"""
        return self.http.get("/info").json()

    def run(self, message: str, session_id: str, user_id: str) -> dict:
        resp = self.http.post(
            f"/agents/{self.agent_id}/runs",
            data={
                "message": message,
                "session_id": session_id,   # 复用即同一线程
                "user_id": user_id,
                "stream": "false",
            },
        )
        resp.raise_for_status()
        return resp.json()


client = AgentOSClient("http://localhost:7777", "support-agent")
print(client.info()["auth_mode"])
reply = client.run("我的订单到哪了？", session_id="order-42", user_id="customer-42")
print(reply["content"])
```

 `clients.py`， BFF ，。

## 23.4 ：/info  /config

：

```bash
curl http://localhost:7777/info
```

：

|  |  |
|---|---|
| `auth_mode` | ：`none` / `security_key` / `jwt` |
| `agent_count` / `team_count` / `workflow_count` |  |
| `mcp.enabled` / `mcp.path` | MCP  |
| `agno_version` |  |

`GET /config`  ID、 ID、。 `/info` ，。

## 23.4 ：

：

```python
agent_os = AgentOS(
    id="product-agent-os",
    agents=[support_agent],
    db=db,
    authorization=True,   # 开启后按 security_key 或 JWT 模式校验请求
)
```

：

- **security_key**：（Bearer token）—— `OS_SECURITY_KEY` ，；
- **JWT**： `JWT_VERIFICATION_KEY`  `JWT_JWKS_FILE` ，/。

```python
# 无需改代码：security_key 模式完全由环境变量驱动（pydantic-settings 读取）
# 启动服务前导出：
#   export AUTHORIZATION_ENABLED=true    # 或构造时传 authorization=True
#   export OS_SECURITY_KEY="你的随机长密钥"   # 作为 Bearer token 校验
agent_os = AgentOS(
    id="product-agent-os",
    agents=[support_agent],
    db=db,
    authorization=True,
)
```

 curl ： 401， `-H "Authorization: Bearer $OS_SECURITY_KEY"` 。 JWT  `OS_SECURITY_KEY`， JWT 。

::: warning 
`auth_mode=none` 。 AgentOS  `authorization=True`， HTTPS —— API Key 。
:::

## 23.5 
AgentOS ""，：

|  |  |
|---|---|
| / | `db=...` |
| JWT  | `authorization=True` |
|  | `tracing=True`（） |
|  MCP Server | `mcp_server=True` |
|  | `scheduler=True` |
|  FastAPI  | `base_app=existing_app` |

`base_app=` ： FastAPI ， `/agents/*` ， Agno 。

## 23.6 

- AgentOS = ， FastAPI ，`get_app()` + `serve()` ；
-  `POST /{component}/{id}/runs`， `session_id` ；
-  `GET /info`（ `auth_mode`），；
-  `authorization=True`；`tracing`、`mcp_server`、`scheduler`、`base_app` 。

<Quiz :items="quiz" />

## 🛠️ 

<script setup>
const quiz = [
  {
    question: '调用 AgentOS 中某个 Agent 的正确 REST 端点是？',
    options: ['GET /run/{agent_id}', 'POST /agents/{agent_id}/runs', 'POST /v1/chat/completions', 'GET /agents/{agent_id}'],
    answer: 1,
    explain: 'Agent/Team/Workflow 的执行端点统一为 POST /agents/{id}/runs、/teams/{id}/runs、/workflows/{id}/runs，message 等参数通过表单字段传递。'
  },
  {
    question: '如何让两次 HTTP 调用属于同一个会话线程？',
    options: ['两次都用相同的 user_id', '复用相同的 session_id', '在 header 里传 X-Thread-Id', '无法做到，每次调用都是新会话'],
    answer: 1,
    explain: '文档说明：响应里返回 session_id，复用它即可把后续 run 归入同一线程；user_id 标识用户而非会话。'
  },
  {
    question: '客户端判断 AgentOS 实例使用哪种鉴权方式，应该调用？',
    options: ['GET /healthz', 'GET /config 里的 version 字段', 'GET /info 看 auth_mode', '发一次不带凭证的请求试错'],
    answer: 2,
    explain: '/info 是公开端点，返回 auth_mode（none/security_key/jwt）等元信息，官方建议客户端据此选择凭证后再发起认证调用。'
  },
  {
    question: '已有大型 FastAPI 项目想引入 AgentOS 而不重写路由，最合适的做法是？',
    options: ['用 base_app=existing_app 把 AgentOS 能力挂进现有应用', '复制现有路由到 AgentOS 生成的 app 里', '放弃 AgentOS 改用裸 Uvicorn', '必须把项目拆成两个独立服务'],
    answer: 0,
    explain: 'AgentOS 提供 base_app 参数支持 Bring Your Own FastAPI App，可在保留既有应用的基础上挂载智能体端点，实现渐进式迁移。'
  }
]
</script>

1.  `uvicorn agent_os:app --port 7777` （ gunicorn  worker）， `session_id` 。
2.  Python  `chat(message, session_id)`， `/agents/support-agent/runs`  SSE 。
3.  `authorization=True`  `/info`  `/agents/*/runs`，、。

> ？[](./ch24)：。
