# Chapter 21 · 人机协同与 Guardrails

> Chapter goals:给 Agent 装上"刹车"——用工具确认机制实现人机协同（HITL），用 pre/post hooks 与内置 Guardrail 拦截敏感输入和危险输出，并设计合理的失败降级策略。

## 21.1 为什么 Agent 需要"刹车"

 Agent ：****（）****（、）。：

1. **（Human-in-the-Loop）**：，；
2. **Guardrails（pre_hooks）**：//；
3. **（post_hooks）**：。

::: info 
Guardrails  hooks  Agno v2.1.0 ；（`requires_confirmation`） HITL 。 Agno 3.x。
:::

## 21.2 工具确认：让危险操作等一个人

 `@tool(requires_confirmation=True)`，Agent ****（`is_paused=True`），， `continue_run()` ：

```python
# hitl_confirm.py
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb          # 续跑需要数据库保存运行状态
from agno.models.openai import OpenAIChat
from agno.tools import tool

@tool(requires_confirmation=True)
def refund_order(order_id: str, amount: float) -> str:
    """为指定订单执行退款，属于敏感资金操作。"""
    return f"订单 {order_id} 已退款 {amount} 元"

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[refund_order],
    db=SqliteDb(db_file="tmp/hitl.db"),      # 必须持久化 run 状态才能续跑
)

run_response = agent.run("帮我把订单 A1001 退款 99 元")

# 审批环节：遍历待确认需求
for requirement in run_response.active_requirements:
    if requirement.needs_confirmation:
        print(f"待确认: {requirement.tool_execution.tool_name}"
              f"{requirement.tool_execution.tool_args}")
        if input("批准吗? (y/n): ").lower() == "y":
            requirement.confirm()
        else:
            requirement.reject()

# 带着审批结果继续执行
response = agent.continue_run(
    run_id=run_response.run_id,
    requirements=run_response.requirements,
)
print(response.content)
```

 Toolkit ， `requires_confirmation_tools=[""]` ，。

## 21.3 内置 Guardrail：PII 与提示注入防护

Guardrail  **pre_hook** ，。Agno  PII 、、OpenAI ：

```python
from agno.agent import Agent
from agno.guardrails import PIIDetectionGuardrail, PromptInjectionGuardrail

support_agent = Agent(
    model=model,
    pre_hooks=[
        PIIDetectionGuardrail(),        # 拦截身份证号/手机号等个人隐私
        PromptInjectionGuardrail(),     # 识别"忽略之前所有指令"类注入攻击
    ],
    markdown=True,
)
# 含手机号的输入会被直接拦截并抛出 InputCheckError
```

## 21.4 自定义 Guardrail

 `BaseGuardrail`  `check()` / `async_check()`（ `.run()`  `.arun()`）， `InputCheckError`：

```python
import re
from agno.exceptions import CheckTrigger, InputCheckError
from agno.guardrails import BaseGuardrail
from agno.run.agent import RunInput

class SensitiveWordGuardrail(BaseGuardrail):
    """拦截包含竞品名的对外咨询输入。"""

    def check(self, run_input: RunInput) -> None:
        if isinstance(run_input.input_content, str):
            if re.search(r"某友|某蝶", run_input.input_content):
                raise InputCheckError(
                    "输入包含不允许讨论的内容。",
                    check_trigger=CheckTrigger.INPUT_NOT_ALLOWED,
                )

    async def async_check(self, run_input: RunInput) -> None:
        self.check(run_input)   # 规则简单时异步版可直接复用同步逻辑

# 用法同内置 guardrail：pre_hooks=[SensitiveWordGuardrail()]
```

 **post_hook**：，、。

```python
from datetime import datetime

def audit_log_hook(run_input, run_output):
    """post_hook：把每次输入与输出落盘，供合规审计。"""
    with open("tmp/audit.log", "a", encoding="utf-8") as f:
        f.write(f"[{datetime.now():%F %T}] IN: {run_input.input_content}\n")
        f.write(f"[{datetime.now():%F %T}] OUT: {run_output.content}\n")

compliance_agent = Agent(
    model=model,
    pre_hooks=[SensitiveWordGuardrail()],   # 事前拦截
    post_hooks=[audit_log_hook],            # 事后审计（也可以做改写/打标）
)
```

## 21.5 失败降级策略

，""：

- ****（）： post_hook ；
- ****： `InputCheckError` ，；
- ****：，；
- ****： guardrail ""，。

## 本章小结

- ：（）、pre_hooks （）、post_hooks （）；
- `@tool(requires_confirmation=True)` + `active_requirements` + `continue_run()`  HITL ， `db` ；
-  Guardrail  `pre_hooks=[...]` ； `BaseGuardrail`  `check()/async_check()`  `InputCheckError`；
- ：、、、。

<Quiz :items="quiz" />

## 🛠️ 动手实践

1. "" `requires_confirmation=True`，→→， Agent 。
2.  Guardrail  500 ， `InputCheckError` 。
3.  Agent  `PIIDetectionGuardrail` ，" + "。

> 下一章把视野扩展到外部世界：[第 22 章：MCP 协议集成](./ch22)。

<script setup>
const quiz = [
  {
    question: '标记 requires_confirmation=True 的工具被触发时，Agent 会发生什么？',
    options: [
      '工具直接跳过不执行',
      '自动执行但在日志中告警',
      '暂停运行（is_paused=True），等待人工通过 continue_run 批准后继续',
      '整个 Agent 抛出异常终止'
    ],
    answer: 2,
    explain: '确认机制会暂停执行并在 run_response.active_requirements 中列出待审批的工具调用，人工 confirm/reject 后调用 continue_run(run_id, requirements) 续跑。'
  },
  {
    question: '使用工具确认机制时为什么必须给 Agent 配置 db？',
    options: [
      '为了缓存模型响应降低成本',
      '为了持久化运行状态，使 continue_run 能按 run_id 恢复上下文',
      '为了存储聊天记录给用户看',
      '不配置 db 功能无法编译'
    ],
    answer: 1,
    explain: '暂停-恢复跨越了两次独立调用，中间状态（含待审批需求）必须落库（如 SqliteDb），continue_run 才能依据 run_id 找回现场。'
  },
  {
    question: '自定义 Guardrail 需要继承哪个类？检测到违规时应如何处理？',
    options: [
      '继承 BaseGuardrail，抛出 InputCheckError',
      '继承 Agent 类，return None',
      '继承 BaseTool，打印警告即可',
      '继承 Model 类，抛出任意异常'
    ],
    answer: 0,
    explain: '官方约定：继承 agno.guardrails.BaseGuardrail 实现 check()/async_check()，违规时抛出带 CheckTrigger 的 InputCheckError，框架据此中断本次运行。'
  },
  {
    question: 'pre_hooks 在什么时机执行？',
    options: [
      '模型开始生成回复之后',
      '会话加载之后、任何 LLM 处理开始之前',
      '工具调用完成之后',
      '仅在流式输出的最后一个分片'
    ],
    answer: 1,
    explain: 'Pre-hook 在会话加载完成后、模型上下文准备与 LLM 执行之前运行，因此能在隐私数据进入模型前完成校验与拦截；输出侧审查则应使用 post_hooks。'
  }
]
</script>
