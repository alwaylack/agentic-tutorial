# Chapter 10 · Team ：

> Chapter goals: Team 、 session state ，。

## 10.1 

 `mode=TeamMode.coordinate`  leader 。Agno （`from agno.team.mode import TeamMode`）：

|  |  |  |
|---|---|---|
| Coordinate（） |  `mode`  `coordinate` | Leader 、、 |
| Route | `mode=TeamMode.route` | Leader ，**** |
| Broadcast | `mode=TeamMode.broadcast` |  |
| Tasks | `mode=TeamMode.tasks` | Leader ， |

::: warning 
 `respond_directly=True`  `delegate_to_all_members=True` （ `route`  `broadcast`），** `mode=`**—— `mode` 。
:::

## 10.2 Route ：

、 leader ， route ：

```python
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.team import Team, TeamMode

model = OpenAIChat(
    id="deepseek-chat",
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

billing_agent = Agent(name="Billing Agent", role="处理账单、退款类问题", model=model)
tech_agent = Agent(name="Tech Agent", role="处理技术故障类问题", model=model)

router_team = Team(
    name="Customer Router",
    model=model,
    mode=TeamMode.route,
    members=[billing_agent, tech_agent],
    # 关键开关：用户原始输入原样传给成员，不再由 leader 改写
    determine_input_for_members=False,
    instructions=["把请求路由到最合适的成员；无法判断时自行回答。"],
)

router_team.print_response("我的订单被扣款两次了", stream=True)
```

`determine_input_for_members=False`  route ： leader ""，。

## 10.3 Broadcast  Tasks 

""，：

```python
from agno.team import Team, TeamMode

# 广播：三个评审员同时收到同一篇稿子
review_team = Team(
    name="Review Panel",
    model=model,
    mode=TeamMode.broadcast,   # 同一任务发给每个成员
    members=[
        Agent(name="Legal Reviewer", role="从合规角度评审", model=model),
        Agent(name="UX Reviewer", role="从用户体验角度评审", model=model),
        Agent(name="SEO Reviewer", role="从搜索优化角度评审", model=model),
    ],
    instructions="汇总所有成员意见，输出一份合并后的修改清单。",
)

# 任务循环：leader 拆解任务清单并逐项推进
ops_team = Team(
    name="Ops Team",
    model=model,
    mode=TeamMode.tasks,
    max_iterations=6,          # 限制任务循环轮数上限，防止失控烧 token
    members=[tech_agent, billing_agent],
)
```

Tasks  `max_iterations`：，。

## 10.4  Session State

（），：**team  `session_state`、 `add_session_state_to_context`、team **：

```python
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat
from agno.team import Team

db = SqliteDb(db_file="tmp/team.db")
model = OpenAIChat(
    id="deepseek-chat",
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

shopping_agent = Agent(
    id="shopping-agent",
    name="Shopping List Agent",
    role="管理购物清单",
    model=model,
    db=db,                                    # 与 team 共用同一 db 才能共享会话
    add_session_state_to_context=True,        # 让成员能看到 session state
    enable_agentic_state=True,                # 允许成员在对话中自主修改 state
)

team = Team(
    name="Household Team",
    members=[shopping_agent],
    session_state={"shopping_list": []},      # 初始共享状态
    db=db,
    add_session_state_to_context=True,
    enable_agentic_state=True,
    description="你们是一个管理购物清单和家务的团队",
    show_members_responses=True,
)

team.print_response("把牛奶、鸡蛋和面包加进购物清单")
team.print_response("鸡蛋我已经买了，现在清单上还有什么？")

print(f"最终状态: {team.get_session_state()}")
```

****——state  instructions，：

```python
team = Team(
    members=[],
    session_state={"user_name": "小明"},
    instructions="用户的名字是 {user_name}，请用名字称呼他。",
    markdown=True,
)
```

## 10.5 ：

""。：

```python
team = Team(
    name="Research Team",
    model=model,
    members=[news_agent, finance_agent],
    markdown=True,
    show_members_responses=True,   # 最终回复中附带各成员的原始回复
)

# stream=True 时可以观察到 leader 委派 -> 成员执行 -> leader 汇总的全过程
for event in team.run("AI 公司最近的动态和 NVDA 股价表现如何？", stream=True):
    print(event)   # 成员启动、工具调用、内容增量都有对应事件
```

：

1. ** `id`**： member id ， name ；
2. ****： `model`  team ——"、leader "；
3. **callable factories**：`members=` ， `session_state` （ `cache_callables=False`）。

## 10.6 

-  `mode=TeamMode.coordinate/route/broadcast/tasks`， `respond_directly` ；
- Route  `determine_input_for_members=False` ；
- ：team  `db` +  `add_session_state_to_context=True` +  `enable_agentic_state=True`；
- instructions  `{var}`  session state；
- `show_members_responses` +  +  member `id` 。

<Quiz :items="quiz" />

## 🛠️ 

<script setup>
const quiz = [
  {
    question: '在 Agno 中，想让「leader 只负责选人并把该成员的回复直接返回」，推荐写法是？',
    options: ['respond_directly=True', 'mode=TeamMode.route', 'delegate_to_all_members=True', 'mode=TeamMode.coordinate'],
    answer: 1,
    explain: '官方文档明确推荐用 mode=TeamMode.route 实现直达回复；respond_directly=True 是遗留写法且会被 mode 覆盖，broadcast 是全员广播，coordinate 是默认的汇总模式。'
  },
  {
    question: 'Route 模式下要把用户原始输入不做改写地传给成员，应该？',
    options: ['设置 determine_input_for_members=False', '设置 share_state=True', '设置 max_iterations=0', '设置 enable_agentic_state=False'],
    answer: 0,
    explain: 'determine_input_for_members=False 表示 leader 不再为成员加工任务描述，直接透传用户输入；其余选项与透传无关。'
  },
  {
    question: '让成员能读取并自主修改团队 session state 的最小配置是？',
    options: [
      '只在 team 上设置 session_state 即可',
      'team 与成员共用同一个 db，并在双方设置 add_session_state_to_context=True',
      '把 state 写进每条用户消息里',
      '必须使用 PostgresDb 才支持共享状态'
    ],
    answer: 1,
    explain: '共享的前提是双方能看到同一份会话数据（共用 db）且都开启 add_session_state_to_context=True；需要模型自主修改时还要开 enable_agentic_state=True，数据库用 SqliteDb 即可。'
  },
  {
    question: '关于 Tasks 模式，下列说法正确的是？',
    options: [
      '它会把同一个任务同时发给所有成员',
      '它会无限循环直到任务完成，无需任何限制',
      'Leader 维护共享任务清单逐项推进，应设置 max_iterations 控制轮数',
      '它等价于 Workflow 的顺序步骤'
    ],
    answer: 2,
    explain: 'Tasks 模式由 leader 构建并执行共享任务列表直到目标达成，max_iterations 用于封顶循环次数防止 token 失控；全员发送是 broadcast 模式的行为。'
  }
]
</script>

1.  route "//"， `determine_input_for_members` 。
2.  10.4 ""，。
3.  `show_members_responses=True`  `stream=True`  coordinate ，（leader →→），。

> ""：，[Workflow](./ch11) 。
