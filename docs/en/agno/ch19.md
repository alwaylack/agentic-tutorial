# Chapter 19 ·  Reasoning 

> Chapter goals: Agno ""——， `reasoning_model`  DeepSeek-R1 ，。

## 19.1 

""：、、，。**（Reasoning）**（、、），——""。

Agno ：

1. **Agent **：`reasoning=True`， Agent （Cop ，`reasoning_min_steps`/`reasoning_max_steps` ）；
2. ****： DeepSeek-R1 / deepseek-reasoner ， `reasoning_model` 。

## 19.2 

： id ，Agent ：

```python
# reasoning_direct.py
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat

reasoning_agent = Agent(
    model=OpenAIChat(
        id="deepseek-reasoner",              # DeepSeek 的思考模型
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    markdown=True,
)

reasoning_agent.print_response("一个 3x3 幻方每行每列和对角线都等于 15，请给出一种填法并验证。")
```

""（），。：**** `temperature` ，。

## 19.3  reasoning_model ""

`reasoning_model` ，——"，"，"，"：

```python
from agno.agent import Agent
from agno.models.openai import OpenAIChat

agent = Agent(
    # 主模型：负责最终回复
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    # 推理模型：遇到难题时先用它逐步推演（也接受 "provider:model_id" 字符串）
    reasoning_model=OpenAIChat(
        id="deepseek-reasoner",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    reasoning_min_steps=1,
    reasoning_max_steps=10,   # 推理步数上限，控制成本
    markdown=True,
)

agent.print_response("三封信随机放进三个信封，全部装错的概率是多少？给出完整推导。")
```

 `reasoning=True`  Agent （`reasoning_agent`  Agent ""），：

```python
stepwise_agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    reasoning=True,          # 开启内置分步推理流程
    reasoning_max_steps=6,   # 限制步数控制成本与耗时
    markdown=True,
)
```

## 19.4 

，"" token ：

|  |  |  |
|---|---|---|
| // | ✅  |  |
| 、 | ✅  |  |
| 、、 | ❌  |  |
|  RAG  | ❌  |  |

::: warning 
-  `reasoning=True` ，""，；
- `reasoning_max_steps`  `run()` ，；
- ，。
:::

## 19.5 、RAG 

：""，RAG ""。 Agentic RAG ——""，，：

```python
agent = Agent(
    model=OpenAIChat(
        id="deepseek-reasoner",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    knowledge=knowledge,          # 第 17 章构建的知识库
    search_knowledge=True,
    instructions=["先分析问题需要哪些信息，再检索知识库，最后作答并给出依据。"],
    markdown=True,
)
```

## 

-  = ，；
- ： id  `deepseek-reasoner` ，；
- `reasoning_model`  Agent （ Model  `provider:model_id` ），`reasoning_min_steps`/`reasoning_max_steps` ；
- ：，；
-  RAG/，。

<Quiz :items="quiz" />

## 🛠️ 

1.  5 ， `deepseek-chat`  `deepseek-reasoner` ，。
2.  `reasoning_model`  Agent， `reasoning_max_steps`  2  10，。
3.  17  RAG Agent ，。

> ""，""：[ 20 ：Skills ](./ch20)；""，[ 21 ： Guardrails](./ch21)。

<script setup>
const quiz = [
  {
    question: 'Agno 中让 Agent 使用"专职推理模型"的参数是？',
    options: ['model="reasoner"', 'reasoning_model', 'thinking_engine', 'reasoning_provider'],
    answer: 1,
    explain: 'reasoning_model 接受一个 Model 对象或 "provider:model_id" 字符串，指定负责推理的模型；主模型仍由 model 参数指定。'
  },
  {
    question: '以下哪类任务最不适合开启推理？',
    options: ['数学竞赛题求解', '多步项目规划', '把一段中文翻译成英文', '复杂合同条款分析'],
    answer: 2,
    explain: '翻译是典型的直接生成任务，思维链几乎不提升质量却成倍增加延迟与 token 成本；数学、规划和复杂分析才是推理的收益场景。'
  },
  {
    question: 'reasoning_min_steps 和 reasoning_max_steps 控制的是什么？',
    options: [
      '模型输出的最大 token 数',
      '内置推理流程的最少/最多推理步数',
      '知识库检索返回的条数',
      '工具调用的并发数'
    ],
    answer: 1,
    explain: '这两个参数约束 Agent 内置推理流程的步数范围，用于平衡推理充分性与耗时成本；它们与输出 token 上限、检索条数无关。'
  },
  {
    question: '关于思考模型的实践，下列说法错误的是？',
    options: [
      '思考模型的输出包含思维链内容，结构化解析时要注意只取最终答案',
      '对简单任务也默认开启推理是稳妥的工程习惯',
      '思考模型延迟明显高于普通模型，不适合实时对话',
      'reasoning=True 与原生思考模型叠加可能造成成本翻倍'
    ],
    answer: 1,
    explain: '推理应当按任务类型按需开启：简单任务开启推理只增加延迟和费用而不提升质量。其余三项都是正确的实践经验。'
  }
]
</script>
