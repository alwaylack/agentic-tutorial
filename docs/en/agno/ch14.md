# Chapter 14 ·  session state  chat history

> Chapter goals:""""， `session_id`、`add_history_to_context`  `session_state` ， `RunContext` 。

## 14.1 

""，：

|  |  |  |  |
|---|---|---|---|
| Chat history（） |  run  | `session_id` |  |
| Session state（） | / | `session_id` | 、、 |
| Memory（） |  | `user_id`（） | 、（ 16 ） |

：**history ""，state ""，memory ""**。。

## 14.2 session_id 

`session_id` 。 `session_id`  run 。""，：

1. ****——（`db=SqliteDb(...)`，）；
2. ****——`add_history_to_context=True`。

```python
# chat_history.py —— 多轮对话记忆
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    db=SqliteDb(db_file="tmp/agent.db"),   # 存储会话 run
    add_history_to_context=True,           # 每次运行注入历史消息
    num_history_runs=3,                    # 只带最近 3 轮
    markdown=True,
)

agent.print_response("我叫小高，我在准备 FastAPI 面试", session_id="chat_001")
agent.print_response("我叫什么？我在准备什么？", session_id="chat_001")  # 能答上
agent.print_response("我叫什么？", session_id="chat_002")               # 换会话：答不上
```

****：，token 。：

|  |  |
|---|---|
| `num_history_runs` |  run（ 3） |
| `num_history_messages` | （， `num_history_runs` ） |
| `max_tool_calls_from_history` |  |

，：

```python
# on_demand_history.py —— 让模型自己决定何时回看历史
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    db=SqliteDb(db_file="tmp/agent.db"),
    read_chat_history=True,   # 模型获得 get_chat_history() 工具
    # 注意：没有 add_history_to_context=True，普通提问不携带历史
)

agent.print_response("我们之前约定过什么暗号吗？", session_id="chat_001")
# 模型判断需要上下文 -> 调用 get_chat_history() -> 基于结果回答
```

：、（，）；， `add_history_to_context` ——。

::: tip 
，， `read_chat_history=True`—— `get_chat_history()` ，****，。
:::

## 14.3 ： Agent ""

`session_state`  dict， `Agent(session_state={...})` 。：**state （），**。

```python
# session_state_basic.py —— 官方购物清单示例的三方模型版
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat
from agno.run import RunContext


def add_item(run_context: RunContext, item: str) -> str:
    """Add an item to the shopping list.

    Args:
        item: The item to add to the shopping list.
    """
    # run_context.session_state 就是当前会话的状态字典
    if run_context.session_state is None:
        run_context.session_state = {}
    run_context.session_state["shopping_list"].append(item)
    return f"The shopping list is now {run_context.session_state['shopping_list']}"


agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    db=SqliteDb(db_file="tmp/agent.db"),
    session_state={"shopping_list": []},      # 新会话的初始状态
    tools=[add_item],
    # instructions 里可以用 {键名} 引用状态，注入时会做模板替换
    instructions="Current state (shopping list) is: {shopping_list}",
    markdown=True,
)

agent.print_response("帮我把牛奶、鸡蛋、面包加进购物清单", session_id="s_100")
print(f"Final session state: {agent.get_session_state()}")   # 代码侧读取
```

：

1. **`RunContext` **： `run_context: RunContext` ，， schema （ 7 ）；
2. ****： `run_context.session_state` ， `session_id`  run ；
3. **instructions **：`{shopping_list}` ，""。

## 14.4 

 state ：

```python
# state_isolation.py —— 验证 state 按 session 隔离
import os
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat
from agno.run import RunContext


def set_counter(run_context: RunContext, value: int) -> str:
    """Set the counter to a value.

    Args:
        value: New counter value.
    """
    run_context.session_state["counter"] = value
    return f"counter = {value}"


agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    db=SqliteDb(db_file="tmp/agent.db"),
    session_state={"counter": 0},
    tools=[set_counter],
    instructions="counter 的当前值是: {counter}",
)

agent.print_response("把计数器设为 42", session_id="session_A")
agent.print_response("计数器现在是多少？", session_id="session_A")  # 42
agent.print_response("计数器现在是多少？", session_id="session_B")  # 0（初始值）
```

`session_A`  `session_B`  state ——"/"。，`run()`  `session_state` （ Agent ）；`enable_agentic_state=True`  JSON ， Agent 。

## 14.5 ： history， state

- **、**（"？"""）→ chat history；
- ****（、、）→ session state；
- ****（""）→ memory（ 16 ）。

 Agent ：memory ，history ，state 。，。

## 

- `session_id` ；history/state ，memory  `user_id` ；
-  =  + `add_history_to_context=True`；token  `num_history_runs` / `num_history_messages` ， `read_chat_history=True` ；
- `session_state` （） `run_context.session_state` ，；instructions  `{}` ；
- state  session ，/。

<Quiz :items="quiz" />

## 🛠️ 

<script setup>
const quiz = [
  {
    question: '让模型在多轮对话中记住上文，最少需要哪两个配置？',
    options: [
      'session_id 和 temperature=0',
      '配置数据库(db=...) 且 add_history_to_context=True',
      'memory=True 和 user_id',
      'debug_mode=True 和 markdown=True'
    ],
    answer: 1,
    explain: '聊天历史必须先落库（db=SqliteDb(...) 等），再通过 add_history_to_context=True 注入上下文；二者缺一不可，与 memory 机制无关。'
  },
  {
    question: 'num_history_runs 和 num_history_messages 同时设置时会怎样？',
    options: [
      '两个条件同时生效取更严格者',
      '直接报错，二者互斥不能同时出现',
      'num_history_runs 生效',
      'num_history_messages 生效'
    ],
    answer: 2,
    explain: '官方文档明确：二者设置其一即可；若同时设置，num_history_runs 优先生效。'
  },
  {
    question: '工具函数中的 run_context.session_state 修改后会发生什么？',
    options: [
      '只在本次函数调用内有效，返回后丢失',
      '自动持久化到数据库，同 session 的后续 run 自动加载',
      '需要手动调用 save_state() 才能保存',
      '会同步到该用户所有会话'
    ],
    answer: 1,
    explain: 'session state 挂在会话上，对 run_context.session_state 的任何修改会随会话自动写入数据库，同一 session_id 的后续运行会加载最新状态；跨会话不共享。'
  },
  {
    question: 'instructions 中写 "当前清单: {shopping_list}"，这个占位符何时生效？',
    options: [
      '作为普通文本原样发给模型',
      '注入上下文时被替换为 session_state 中 shopping_list 的当前值',
      '需要模型主动调用 get_state 工具才能看到',
      '仅在 debug_mode 下生效'
    ],
    answer: 1,
    explain: 'instructions/description 支持用 {键名} 引用会话状态，Agno 在构造系统消息时做模板替换，让模型始终看到最新状态值。'
  }
]
</script>

1.  14.3  `remove_item(item: str)` ，""， instructions  `{shopping_list}` 。
2.  10 ， `num_history_runs=1`  `=5` ， `response.metrics`  token 。
3. ""： session_state //， `book_ticket()` （： state）。

> ，[ 15 ： Storage——](./ch15)。
