# Chapter 7 · tool Tools：tool

> Chapter goals：Understand Agno tool，tool，"modeltool"。

## 7.1  Agent tool

model——、、。**tool（Tools）** ： Python  Agent，model""，Agno model。

::: tip 
tool，：modeloutput" `get_weather(city='')`"→  →  → model。，modeltool、。
:::

documentation：

1. Agent **tool（schema）**model；
2. model，/tool；
3. Agno ****tool；
4. toolmodel；
5. ，model。

## 7.2 tool Schema

：**Agno 、docstring tool schema**。：

```python
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat


def lookup_order(order_id: str) -> dict:
    """Return the status and delivery date for an order.

    Args:
        order_id: The order ID to look up.
    """
    return {
        "order_id": order_id,
        "status": "shipped",
        "delivery_date": "2026-07-22",
    }


agent = Agent(
    #  OpenAI model
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[lookup_order],  # 
    instructions="Use the order tool when a customer asks about a delivery.",
)

agent.print_response(" ORD-123 ？")
```

model JSON Schema，：

```json
{
  "name": "lookup_order",
  "description": "Return the status and delivery date for an order.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "The order ID to look up."
      }
    },
    "required": ["order_id"]
  }
}
```

tool（ "Design Tools for Reliable Calls"）：

- ****：`lookup_order`  `do_something`，modeltool；
- **docstring ""**：modelnote；
- ** + note**： schema 。

## 7.3 tool Toolkit 

tool，（、、）。Agno  **Toolkit**，、。 `DuckDuckGoTools` ：

```bash
pip install -U agno ddgs   # DuckDuckGoTools  ddgs 
```

```python
# search_agent.py ——  Agent 
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.duckduckgo import DuckDuckGoTools

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[DuckDuckGoTools()],
    instructions=[""],
    markdown=True,
    debug_mode=True,  # v2 tool：run
)

agent.print_response(" AI Agent ？")
```

run：

```text
● Tool call: web_search(query="AI Agent  ")      ← model
● Tool result: [{"title": "...", "url": "..."}, ...]          ← 
● （）
```

> note：Agno  2.x  `show_tool_calls=True` （3.x ）。tool， `debug_mode=True`；streaming `RunContentEvent` toolevent。

tool `YFinanceTools`（）、`HackerNewsTools`、`Newspaper4kTools`（）、`FileTools`() ， Toolkit Index。：

```python
# stock_agent.py —— tool + tool
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.duckduckgo import DuckDuckGoTools
from agno.tools.yfinance import YFinanceTools

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[
        YFinanceTools(enable_stock_price=True, enable_analyst_recommendations=True),
        DuckDuckGoTools(),
    ],
    instructions=[
        "tool",
        "，",
        "",
    ],
    markdown=True,
    debug_mode=True,
)

agent.print_response(" AMD ")
```

## 7.4 tool

Toolkit ，，model。 `DuckDuckGoTools` ：

```python
# toolkit_params.py —— tool
from agno.tools.duckduckgo import DuckDuckGoTools

tools = DuckDuckGoTools(
    enable_news=False,       # ， web_search
    fixed_max_results=3,     #  3 
    region="cn-zh",          # 
    timeout=15,              # 
)
```

，Agent ：

```python
agent = Agent(
    # ...model configuration...
    tools=[YFinanceTools(all=True)],
    include_tools=["get_current_stock_price"],  # 
    # exclude_tools=["get_company_profile"],    # 
)
```

::: warning tool
tool，model，（documentation "Expose only the tools needed for the task"）。， 2 tool Agent  20 tool。
:::

## 7.5 tool

Agno ，，（、） 21  Guardrails ：

| configuration |  |
|---|---|
| `tool_call_limit=N` |  run  N tool， |
| `read_chat_history=True` | model `get_chat_history()` tool， |
| tool `run_context` | run（、session state ），model schema |

`run_context` Understand（）——tool，Agno ，**model**：

```python
# run_context_demo.py —— 
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.run import RunContext


def who_is_calling(run_context: RunContext) -> str:
    """Get the current user id of this session."""
    # run_context ，model
    return f": {run_context.user_id or ''}"


agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[who_is_calling],
)

agent.print_response("？", user_id="gao", session_id="s1")
```

 `agent`、`team`、 `images / videos / audios / files`（runinput）。

## Summary

- tool =  Agent  Python ；" →  →  → "；
- ** + docstring + **modeltool schema，；
-  Toolkit （`DuckDuckGoTools`、`YFinanceTools` ），；
-  `include_tools` / `exclude_tools` ；tool；
- v2  tool call  `debug_mode=True`（`show_tool_calls` ）；`run_context: RunContext` 。

<Quiz :items="quiz" />

## 🛠️ Practice

<script setup>
const quiz = [
  {
    question: 'Agno  Python tool schema？',
    options: [
      '、docstring ',
      ' JSON Schema ',
      '',
      ' agent.register_tool() '
    ],
    answer: 0,
    explain: 'Agno 、docstring（）（ schema）toolmodel， JSON Schema。'
  },
  {
    question: 'Agno  Agent tool，？',
    options: [
      ' show_tool_calls=True',
      ' debug_mode=True ',
      '',
      'tool'
    ],
    answer: 1,
    explain: 'v2  show_tool_calls ；debug_mode=True outputtoolrun，streamingevent。'
  },
  {
    question: 'tool run_context: RunContext ，？',
    options: [
      'tool schema，model',
      '，model schema ',
      ' Toolkit tool run_context',
      'run_context  API '
    ],
    answer: 1,
    explain: 'run_context ""，Agno run（、session state ），model schema 。'
  },
  {
    question: ' Agent  20 toolmodeltool，？',
    options: [
      ' temperature  0',
      'model 20 tool',
      ' include_tools/exclude_tools tool',
      'tool docstring  prompt'
    ],
    answer: 2,
    explain: 'Practicetool：tool。model； docstring modeltool。'
  }
]
</script>

1.  `word_count(text: str) -> dict` tool（/）， Agent ""， `debug_mode=True` tool。
2. Install `ddgs`， `DuckDuckGoTools(fixed_max_results=3)` ""Agent，。
3.  7.5  Agent  `tool_call_limit=3`，（" 5 "）， Agent 。

> Practice，[ 8 ：tool Toolkit ](./ch08)。
