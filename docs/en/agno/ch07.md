# Chapter 7 · Tools: Built-in Toolkit Principles and Usage

> Chapter goals: Agno ，，""。

## 7.1  Agent 

——、、。**（Tools）** ： Python  Agent，""，Agno 。

::: tip 
，：" `get_weather(city='')`"→  →  → 。，、。
:::

：

1. Agent **（schema）**；
2. ，/；
3. Agno ****；
4. ；
5. ，。

## 7.2  Schema

：**Agno 、docstring  schema**。：

```python
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat


def lookup_order(order_id: str) -> dict:
    """Return the status and delivery date for an order.

    Args:
        order_id: The order ID to look up.
    """
    return {
        "order_id": order_id,
        "status": "shipped",
        "delivery_date": "2026-07-22",
    }


agent = Agent(
    # 统一使用三方 OpenAI 兼容模型
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[lookup_order],  # 直接传函数即可
    instructions="Use the order tool when a customer asks about a delivery.",
)

agent.print_response("订单 ORD-123 什么时候到？")
```

 JSON Schema，：

```json
{
  "name": "lookup_order",
  "description": "Return the status and delivery date for an order.",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {
        "type": "string",
        "description": "The order ID to look up."
      }
    },
    "required": ["order_id"]
  }
}
```

（ "Design Tools for Reliable Calls"）：

- ****：`lookup_order`  `do_something`，；
- **docstring ""**：；
- ** + **： schema 。

## 7.3  Toolkit 

，（、、）。Agno  **Toolkit**，、。 `DuckDuckGoTools` ：

```bash
pip install -U agno ddgs   # DuckDuckGoTools 需要 ddgs 库
```

```python
# search_agent.py —— 让 Agent 联网搜索
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.duckduckgo import DuckDuckGoTools

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[DuckDuckGoTools()],
    instructions=["回答时附带信息来源链接"],
    markdown=True,
    debug_mode=True,  # v2 中观察工具调用的方式：打印详细运行日志
)

agent.print_response("最近一周 AI Agent 领域有什么大事？")
```

：

```text
● Tool call: web_search(query="AI Agent 大事 最近一周")      ← 模型发起
● Tool result: [{"title": "...", "url": "..."}, ...]          ← 函数执行结果
● 最终回答（引用了搜索结果的链接）
```

> ：Agno  2.x  `show_tool_calls=True` （3.x ）。， `debug_mode=True`； `RunContentEvent` 。

 `YFinanceTools`（）、`HackerNewsTools`、`Newspaper4kTools`（）、`FileTools`() ， Toolkit Index。：

```python
# stock_agent.py —— 行情工具 + 搜索工具协作
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.duckduckgo import DuckDuckGoTools
from agno.tools.yfinance import YFinanceTools

agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[
        YFinanceTools(enable_stock_price=True, enable_analyst_recommendations=True),
        DuckDuckGoTools(),
    ],
    instructions=[
        "金融数据优先使用行情工具获取实时数据",
        "行业新闻用搜索引擎查询，并注明来源",
        "用表格展示数据对比",
    ],
    markdown=True,
    debug_mode=True,
)

agent.print_response("对比英伟达和 AMD 的最新股价与分析师评级")
```

## 7.4 

Toolkit ，，。 `DuckDuckGoTools` ：

```python
# toolkit_params.py —— 精确控制工具包行为
from agno.tools.duckduckgo import DuckDuckGoTools

tools = DuckDuckGoTools(
    enable_news=False,       # 关闭新闻搜索，只留 web_search
    fixed_max_results=3,     # 固定最多返回 3 条结果
    region="cn-zh",          # 区域设置
    timeout=15,              # 超时秒数
)
```

，Agent ：

```python
agent = Agent(
    # ...model 等配置同上...
    tools=[YFinanceTools(all=True)],
    include_tools=["get_current_stock_price"],  # 只暴露白名单内的函数
    # exclude_tools=["get_company_profile"],    # 或者排除指定函数
)
```

::: warning 
，，（ "Expose only the tools needed for the task"）。， 2  Agent  20 。
:::

## 7.5 

Agno ，，（、） 21  Guardrails ：

|  |  |
|---|---|
| `tool_call_limit=N` |  run  N ， |
| `read_chat_history=True` |  `get_chat_history()` ， |
|  `run_context` | （、session state ）， schema |

`run_context` （）——，Agno ，****：

```python
# run_context_demo.py —— 框架注入的隐藏参数
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.run import RunContext


def who_is_calling(run_context: RunContext) -> str:
    """Get the current user id of this session."""
    # run_context 由框架注入，模型看不到它
    return f"当前用户: {run_context.user_id or '匿名'}"


agent = Agent(
    model=OpenAIChat(
        id="deepseek-chat",
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com/v1",
    ),
    tools=[who_is_calling],
)

agent.print_response("我是谁？", user_id="gao", session_id="s1")
```

 `agent`、`team`、 `images / videos / audios / files`（）。

## 

-  =  Agent  Python ；" →  →  → "；
- ** + docstring + ** schema，；
-  Toolkit （`DuckDuckGoTools`、`YFinanceTools` ），；
-  `include_tools` / `exclude_tools` ；；
- v2  tool call  `debug_mode=True`（`show_tool_calls` ）；`run_context: RunContext` 。

<Quiz :items="quiz" />

## 🛠️ 

<script setup>
const quiz = [
  {
    question: 'Agno 根据什么为普通 Python 函数生成工具 schema？',
    options: [
      '函数名、docstring 与类型注解',
      '需要手写一份 JSON Schema 文件',
      '根据函数所在模块名自动猜测',
      '只在显式调用 agent.register_tool() 后生效'
    ],
    answer: 0,
    explain: 'Agno 自动从函数名、docstring（作为描述）和类型注解（作为参数 schema）构建工具定义并交给模型，无需手写 JSON Schema。'
  },
  {
    question: 'Agno 中想观察 Agent 的工具调用过程，正确做法是？',
    options: [
      '设置 show_tool_calls=True',
      '设置 debug_mode=True 查看详细日志',
      '只能通过数据库审计日志查看',
      '工具调用过程无法观察'
    ],
    answer: 1,
    explain: 'v2 已移除 show_tool_calls 参数；debug_mode=True 会输出包含工具调用与结果的详细运行日志，流式场景也可监听事件。'
  },
  {
    question: '关于在工具中声明 run_context: RunContext 参数，下列说法正确的是？',
    options: [
      '该参数会被写入工具 schema，模型必须传值',
      '该参数由框架在执行时自动注入，且不会出现在发给模型的 schema 里',
      '只有 Toolkit 类中的工具才能使用 run_context',
      'run_context 只能用来读取 API 密钥'
    ],
    answer: 1,
    explain: 'run_context 属于"内建注入参数"，Agno 在执行时自动传入当前运行的上下文（用户、session state 等），并在发送给模型前将其从 schema 中剔除。'
  },
  {
    question: '某 Agent 挂了 20 个工具但模型经常选错工具，最有效的改进是？',
    options: [
      '把 temperature 调到 0',
      '换更大的模型并保持 20 个工具不变',
      '用 include_tools/exclude_tools 只保留任务真正需要的少数工具',
      '把所有工具的 docstring 删掉以简化 prompt'
    ],
    answer: 2,
    explain: '官方最佳实践明确要求只暴露任务所需的工具：工具越多选择歧义越大。裁剪暴露面比堆模型能力更有效；删除 docstring 反而会让模型更难正确使用工具。'
  }
]
</script>

1.  `word_count(text: str) -> dict` （/）， Agent ""， `debug_mode=True` 。
2.  `ddgs`， `DuckDuckGoTools(fixed_max_results=3)` ""Agent，。
3.  7.5  Agent  `tool_call_limit=3`，（" 5 "）， Agent 。

> ，[ 8 ： Toolkit ](./ch08)。
