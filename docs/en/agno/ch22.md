# Chapter 22 · MCP 

> Chapter goals: MCP（Model Context Protocol）， `MCPTools`  stdio/SSE/Streamable HTTP  MCP ， Agent，。

## 22.1 MCP 

，（GitHub、、……）。**MCP（Model Context Protocol）** Anthropic ： **MCP Server**， MCP （Agent 、IDE、）。

：

-  MCP："Agent × "，N×M ；
-  MCP：Server （N+M），。

Agno  MCP （`from agno.tools.mcp import MCPTools`），。

## 22.2 Stdio  Server

**stdio** ：Agno  MCP Server ，，（、Git、）。 filesystem server ：

```python
# mcp_fs.py
import asyncio
import os
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.mcp import MCPTools

async def run():
    # 以子进程方式启动 npx @modelcontextprotocol/server-filesystem，
    # 并限定它只能访问 ./docs 目录
    mcp_tools = MCPTools(command="npx -y @modelcontextprotocol/server-filesystem ./docs")
    await mcp_tools.connect()          # 连接并拉取该 server 提供的工具清单
    try:
        agent = Agent(
            model=OpenAIChat(
                id="deepseek-chat",
                api_key=os.getenv("DEEPSEEK_API_KEY"),
                base_url="https://api.deepseek.com/v1",
            ),
            tools=[mcp_tools],         # 像普通工具一样传入即可
            markdown=True,
        )
        await agent.aprint_response("列出 docs 目录下有哪些 Markdown 文件，并总结第一个文件的目录结构")
    finally:
        await mcp_tools.close()        # 务必关闭连接、回收子进程

asyncio.run(run())
```

：

```bash
pip install "agno[mcp]"      # 安装 MCP 客户端依赖
npx --version                # 需要Node.js 环境运行 npx 启动的 server
```

## 22.3  Transport 

| Transport |  |  |
|---|---|---|
| **stdio** |  | //Git  |
| **SSE** | HTTP(S)  |  MCP  |
| **Streamable HTTP** |  HTTP  | ，、 |

 URL  command， SSE  `MCPTools(url="https://example.com/sse")` ； server 。

```python
import asyncio
from agno.tools.mcp import MCPTools

async def remote_tools():
    # 远程 server：SSE 传输，凭证走环境变量而非硬编码
    sse_tools = MCPTools(url="https://mcp.example.com/sse")
    await sse_tools.connect()
    try:
        yield sse_tools        # 在 FastAPI 请求生命周期内复用连接
    finally:
        await sse_tools.close()
```

::: tip  Server 
 `MultiMCPTools`  server，—— `MCPTools`  `tools=[...]` ，。
:::

## 22.4 

MCP  `@tool` ，：

```python
agent = Agent(
    model=model,
    tools=[
        mcp_tools,                       # 来自 MCP server 的外部工具
        get_current_user,                # 自己写的内部函数工具
        DuckDuckGoTools(),               # Agno 内置工具包
    ],
)
```

 MCP ：** server，**，。

## 22.5 

MCP ，：

1. ****：filesystem server ； server ；
2. ****： server ，`npx -y` ，；
3. ****： MCP  21  guardrail；
4. ****： server  URL /，。

```python
from agno.guardrails import PIIDetectionGuardrail

# 把第 21 章的防线叠加到 MCP 工具上
safe_agent = Agent(
    model=model,
    tools=[mcp_tools],                      # 外部 server 的工具池
    pre_hooks=[PIIDetectionGuardrail()],    # 敏感信息不进模型，也不进外部工具
    instructions=["调用文件工具前先向用户确认目标路径。"],
)
```

## 

- MCP "、"， N×M  N+M；
- `MCPTools(command=...)` + `connect()`/`close()`  stdio  server； SSE  Streamable HTTP；
-  server  `MCPTools` （`MultiMCPTools` ）；
- MCP 、 Toolkit  `tools=[...]`；
-  MCP ：、、。

<Quiz :items="quiz" />

## 🛠️ 

1.  stdio  filesystem server， Agent 。
2.  Git MCP server（`uvx mcp-server-git`）， Agent  5 。
3.  MCP  `@tool` ，" README  API"。

>  Agent ，：[ 23 ：AgentOS](./ch23)， Agent ；[ 9–10 ](./ch09)。

<script setup>
const quiz = [
  {
    question: 'MCP 协议解决的核心问题是？',
    options: [
      '统一各家模型的 API 格式',
      '让工具提供方实现一次服务，所有支持 MCP 的客户端都能复用其工具',
      '压缩提示词长度',
      '加速模型推理速度'
    ],
    answer: 1,
    explain: 'MCP 标准化了"工具如何被发现和调用"，把 N 个客户端 × M 个系统的定制集成（N×M）简化为各自实现一次协议（N+M）。'
  },
  {
    question: '在 Agno 中通过 stdio 连接本地 MCP server 的正确做法是？',
    options: [
      'MCPTools(url="./server.py")',
      'MCPTools(command="npx -y some-server") 后调用 connect()',
      'Agent(mcp="some-server")',
      '把 server 路径写进 instructions 里'
    ],
    answer: 1,
    explain: 'stdio 传输通过 command 参数指定启动 server 的命令，MCPTools 会将其作为子进程管理；connect() 建立会话，close() 回收资源。url 参数用于远程传输。'
  },
  {
    question: '需要同时连接多个 MCP server 时，官方现在的建议是？',
    options: [
      '使用 MultiMCPTools',
      '在一个 tools 列表中放入多个独立的 MCPTools 实例',
      '串行多次实例化同一个 Agent',
      '把多个 server 的工具手动复制粘贴到代码里'
    ],
    answer: 1,
    explain: 'MultiMCPTools 已被官方标记为废弃，推荐改为创建多个 MCPTools 实例并列传入 tools=[...]，各 server 生命周期独立可控。'
  },
  {
    question: '关于 MCP 安全实践，下列说法错误的是？',
    options: [
      'filesystem server 应只开放必要目录',
      '生产环境应对 npx 启动的第三方 server 固定版本号',
      '只要 server 能跑通就不需要审计其来源',
      '危险的 MCP 工具可结合用户确认机制保护'
    ],
    answer: 2,
    explain: 'npx -y 会自动下载并执行第三方代码，不审计来源等于把服务器控制权交给陌生人；正确做法是选用可信包并锁定版本。'
  }
]
</script>
