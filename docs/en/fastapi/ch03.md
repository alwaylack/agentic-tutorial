# Chapter 3 · Path Parameters and Query Parameters

> Chapter goals: master path parameter type conversion and validation, optional/required/list query parameter declarations, and FastAPI's recognition rules and common pitfalls when mixing both.

## 3.1 Path Parameters and Automatic Type Conversion

The `{}` placeholders in the path are path parameters; FastAPI parses and validates them according to the function signature's type annotations:

```python
from fastapi import FastAPI

app = FastAPI()


@app.get("/items/{item_id}")
def read_item(item_id: int):
    # annotated as int: auto-converts when requesting /items/5;
    # returns 422 validation error when requesting /items/abc, never enters the function body
    return {"item_id": item_id}
```

Requesting `/items/abc` yields a structured error (HTTP status 422):

```json
{
  "detail": [
    {
      "type": "int_parsing",
      "loc": ["path", "item_id"],
      "msg": "Input should be a valid integer, unable to parse string as an integer"
    }
  ]
}
```

This is "types as validation": every type annotation you write serves as both a runtime defense and documentation.

## 3.2 Constrain Values with Enum

To restrict a path parameter to a fixed set of values, use an enum:

```python
from enum import Enum
from fastapi import FastAPI

app = FastAPI()


class ModelName(str, Enum):
    # inherit str so members participate directly in string comparison and serialization
    alexnet = "alexnet"
    resnet = "resnet"
    lenet = "lenet"


@app.get("/models/{model_name}")
def get_model(model_name: ModelName):
    if model_name is ModelName.alexnet:
        return {"model": model_name, "message": "Deep Learning FTW!"}
    return {"model": model_name, "message": "Have some residuals"}
```

## 3.3 Query Parameters

Parameters not in the path become query parameters by default:

```python
from fastapi import FastAPI

app = FastAPI()


@app.get("/items/")
def read_items(skip: int = 0, limit: int = 10):
    return {"skip": skip, "limit": limit}
```

访问 `/items/?skip=5&limit=20` 即可传入参数。可选参数给默认值，必填参数不给默认值：

```python
@app.get("/items/{item_id}")
def read_item(item_id: int, q: str | None = None):
    # q 可选；item_id 必填（路径参数天然必填）
    if q:
        return {"item_id": item_id, "q": q}
    return {"item_id": item_id}
```

::: tip 参数识别规则
FastAPI 按"路径占位符 vs 函数参数名"区分路径参数和查询参数：
- 函数参数名与路径 `{}` 匹配 → 路径参数
- 函数参数名不在路径中 → 查询参数（有默认值为可选，无默认值为必填）
:::

## 3.4 本章小结

- 路径参数用 `{param}` 声明，类型注解驱动自动转换与校验；
- 枚举类约束固定取值，避免非法字符串进入业务逻辑；
- 非路径参数默认为查询参数，默认值决定可选/必填；
- 路由匹配顺序：具体路径在前，泛化路径在后。

<Quiz :items="quiz" />

## 🛠️ Hands-On Practice

1. 创建一个 `/users/{user_id}` 路由，user_id 类型为 int，访问 `/users/abc` 验证 422 错误。
2. 用 Enum 定义一个 `/models/{name}` 路由，name 只能取 alexnet/resnet/lenet 之一。
3. 为 `/items/` 添加可选查询参数 `search: str | None = None`，在响应中返回搜索词。

<script setup>
const quiz = [
  {
    question: 'FastAPI 中路径参数的类型注解作用是？',
    options: ['只用于文档生成', '自动类型转换与运行时校验', '不影响请求处理', '仅用于类型检查工具'],
    answer: 1,
    explain: 'FastAPI 的类型注解同时用于参数解析、运行时校验和文档生成——"类型即校验"。'
  },
  {
    question: '以下哪种写法能让查询参数变为必填？',
    options: ['给默认值 None', '不给默认值', '用 Optional 标注', '用 Query 包装'],
    answer: 1,
    explain: '查询参数有默认值则为可选，无默认值则为必填（path parameter 天然必填）。'
  },
  {
    question: 'Enum 路径参数的优势是？',
    options: ['性能更好', '限制取值范围并自动校验', '支持正则匹配', '可以返回 JSON'],
    answer: 1,
    explain: '继承 str 的 Enum 既限制取值又参与字符串比较/序列化，非法值自动返回 422。'
  },
  {
    question: '路由 `/items/{item_id}` 和 `/items/special` 同时存在时，正确的声明顺序是？',
    options: ['泛化路径在前', '具体路径在前', '顺序不影响', '必须放在不同文件'],
    answer: 1,
    explain: 'FastAPI 按声明顺序匹配，具体路径在前才能被正确命中，否则会被泛化路径拦截。'
  }
]
</script>
